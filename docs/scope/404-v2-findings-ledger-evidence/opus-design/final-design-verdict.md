Both Day captures now show the full day chart retained — glucose, evidence, insulin, basal Δ and context lanes drawn — over a dimmed stage with the navigator and reading pane in place, at 1280 and 1440. Read both images successfully; they are updated files (stamped 20:32, against 20:19 in the pair I saw last round), replaced in place rather than added.

**The outstanding Day correction is fixed.** `retainLoadingFrame` = `markRetainedFrameLoading` + `mountCharts`, which is the re-seat through Day's existing chart owner I asked for, not a second chart path. `mountCharts` guards on `memory.day`, so a first-ever load with nothing to retain still falls through to the skeleton, and its `hold(() => observer.disconnect())` / `hold(() => chart.dispose())` registrations are re-made each pass, so nothing accumulates. Loading stays visible: the `.55` dim is still applied over the retained evidence in both captures.

**The requested design corrections are ready.** All prior dispositions stand — v1 base Filter, no invented 120, visible withholding and retry reason, honest no-route status, routed View Plan, header guards, follow-chip sizing, cohort row readability, distinct Later conclusion field, failure no longer styled as metadata — and the two cosmetic items I left open are closed: the CSS comment now describes the real cascade (shared box and state mechanics, desk ink and expanded material from the bridge rule), and the v1 capture carries its true 1440×900 name.

**Marker-owner disposition: accepted.** `mealMemberMarkers` takes history-run payloads and always resolves to a value, parking on its declared rail; the selected Pattern chart takes singular case-file markers and must omit a marker whose glucose is unknown. Those are two different missing-evidence contracts on two different payload shapes, so reusing the history owner would mean either changing its contract or branching it — the charter's reuse rule covers behavior already reachable through a module's interface, and "omit" is not reachable through that one. What remains shared is about four lines of nearest-observed-point search, which is not worth a seam.

No remaining required defect. One non-blocking note: each render while a read is held disposes and re-initializes the ECharts instance, so a read spanning several renders rebuilds the chart more than once. Node identity and series survive it — the regression asserts nonempty series before, during and after — so this is a cost, not a correctness gap, and I would not change it in #404.
