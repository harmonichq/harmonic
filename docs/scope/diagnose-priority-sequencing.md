# Diagnose priority sequencing

## Decisions

- Research the current GitHub backlog and shipped engine/UI seams before opening new Diagnose design work. The immediate uncertainty is whether existing engine or safety issues block useful data exploration; answering that makes the surface decisions tractable. `inline`
- #52 and #54 are already closed, and #61 through #64 have shipped the current
  excursion chips, outcome-anchored By event membership, highs account, and shared
  browser population. There is no remaining event-projection prerequisite. `inline`
- #13 is the only existing hard prerequisite: close the reachable correction-factor
  staging path before broadening Diagnose actions or ranking that row differently.
  `→ issue` ([#13](https://github.com/harmonichq/harmonic/issues/13))
- The finished-Trial handoff is a distinct unfiled lifecycle defect: the detail says a
  Focus is available at completion, while the 28-day active-Trial guard still rejects
  it and Keep records nothing. Settle durable Trial resolution and next-action routing
  before treating the workflow as complete. `→ issue`
- Use the existing server-owned 0–100 Priority as the first card-ranking currency and
  make its magnitudes legible through #60 plus the actionability half of #26. `→ issue`
- A matched-versus-not-matched care score requires its own engine/measurement contract.
  The event projection publishes supported curves and cohorts, not a comparable or
  causal outcome delta; the frontend must not derive one. `→ issue`
- Diagnose iteration may proceed in parallel with the I:C engine lane (#10 → #21 →
  #23, with #24 deferred). That lane changes when I:C may assert, but it does not block
  slicing and navigating already-published evidence. `inline`
- Immediate usability order after #13 and the Trial handoff: #6, #60, ranking half of
  #26, then #3 and #2. `inline`

## Open questions

- #13 still needs Connor's product ruling on whether the no-programmed read keeps the
  robust night median visible; the backend staging closure is not contingent on showing
  it as a recommendation.
- #26 still needs the queue-rank ruling separated from its correction-factor canvas
  decision.
- The new Trial-resolution issue must settle what Keep records and whether completion
  routes directly to one next eligible Focus/setting or returns to a ranked chooser.

## Spawned tasks

- Repository and GitHub issue research: complete —
  [`docs/research/diagnose-priority-sequencing.md`](../research/diagnose-priority-sequencing.md).
