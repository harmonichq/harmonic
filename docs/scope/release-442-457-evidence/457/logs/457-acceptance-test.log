..RUN pytest: uv run python -m pytest tests/test_empty.py
pytest: exit 5, 0.000 s
.RUN pytest: uv run python -m pytest tests/test_one.py tests/test_two.py
pytest: exit 0, 0.000 s
.RUN case-cache: node $TMPDIR/tmp0i6qcq5y/out/case-cache.mjs
case-cache: exit 1, 0.073 s
RUN case-cache: node $TMPDIR/tmpe93gn0pu/out/case-cache.mjs
case-cache: exit 1, 0.085 s
.RUN case-cache: node $TMPDIR/tmp_tclwto7/out/case-cache.mjs
case-cache: exit 0, 1.083 s
............................ledger inventory: {'issued': 171, 'active': 153, 'retired': 18}
ledger inventory: {'issued': 171, 'active': 152, 'retired': 19}
ledger=171 registry=171 missing=[] extra=[]
latest nightly 123: success
nightly success: refreshed 2 PR commit statuses
latest nightly 123: success
nightly success: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
::warning::No scheduled nightly has completed yet; bootstrap passes until the first completed run.
latest nightly 123: success
# executed 130 · failed 0 · deferred 0 · selected 130
# executed 130 · failed 0 · deferred 0 · selected 130
# executed 32 · failed 0 · deferred 0 · selected 32
# executed 33 · failed 0 · deferred 0 · selected 33
# executed 32 · failed 0 · deferred 0 · selected 32
# executed 33 · failed 0 · deferred 0 · selected 33
# executed 20 · failed 0 · deferred 0 · selected 20
# executed 130 · failed 0 · deferred 0 · selected 130
# executed 130 · failed 0 · deferred 0 · selected 130
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpc6ues0ri/replay-import-sources.json
replay-imports: exit 0, 0.086 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpc6ues0ri/replay-import-sources.json
replay-imports: exit 0, 0.089 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpc6ues0ri/replay-import-sources.json
replay-imports: exit 0, 0.084 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpc6ues0ri/replay-import-sources.json
replay-imports: exit 0, 0.082 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpc6ues0ri/replay-sources.json
replay-graph: exit 0, 0.090 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.086 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp3b2uwhxf/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp3b2uwhxf/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp3b2uwhxf/replay-import-sources.json
replay-imports: exit 0, 0.087 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp3b2uwhxf/replay-import-sources.json
replay-imports: exit 0, 0.073 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmp3b2uwhxf/replay-sources.json
replay-graph: exit 0, 0.084 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpt67cmpym/replay-import-sources.json
replay-imports: exit 0, 0.082 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpt67cmpym/replay-import-sources.json
replay-imports: exit 0, 0.090 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpvpxvol3c/replay-import-sources.json
replay-imports: exit 0, 0.080 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpvpxvol3c/replay-import-sources.json
replay-imports: exit 0, 0.079 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpvpxvol3c/replay-import-sources.json
replay-imports: exit 0, 0.078 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpvpxvol3c/replay-import-sources.json
replay-imports: exit 0, 0.087 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpvpxvol3c/replay-sources.json
replay-graph: exit 0, 0.082 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.079 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmphyol4jhu/replay-import-sources.json
replay-imports: exit 0, 0.087 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmphyol4jhu/replay-import-sources.json
replay-imports: exit 0, 0.080 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmphyol4jhu/replay-import-sources.json
replay-imports: exit 0, 0.090 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmphyol4jhu/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmphyol4jhu/replay-sources.json
replay-graph: exit 0, 0.079 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.082 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpgtqc2s4r/replay-import-sources.json
replay-imports: exit 0, 0.084 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp2vfi__db/replay-import-sources.json
replay-imports: exit 0, 0.078 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpwlywvstn/replay-import-sources.json
replay-imports: exit 0, 0.082 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp__dmp7ou/replay-import-sources.json
replay-imports: exit 0, 0.073 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp5e6ayf96/replay-import-sources.json
replay-imports: exit 0, 0.084 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpf_vb72du/replay-import-sources.json
replay-imports: exit 0, 0.079 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpd4w7749u/replay-import-sources.json
replay-imports: exit 0, 0.086 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpd4w7749u/replay-import-sources.json
replay-imports: exit 0, 0.079 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpd4w7749u/replay-import-sources.json
replay-imports: exit 0, 0.079 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpd4w7749u/replay-import-sources.json
replay-imports: exit 0, 0.080 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpd4w7749u/replay-sources.json
replay-graph: exit 0, 0.074 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.084 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp614u9rp2/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp614u9rp2/replay-import-sources.json
replay-imports: exit 0, 0.091 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmp614u9rp2/replay-sources.json
replay-graph: exit 0, 0.091 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.086 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmppj_uxjhp/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmppj_uxjhp/replay-import-sources.json
replay-imports: exit 0, 0.083 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmppj_uxjhp/replay-sources.json
replay-graph: exit 0, 0.091 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.081 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpgwolrlu9/replay-import-sources.json
replay-imports: exit 0, 0.082 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpgwolrlu9/replay-import-sources.json
replay-imports: exit 0, 0.080 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpgwolrlu9/replay-sources.json
replay-graph: exit 0, 0.087 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.087 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpq6odk021/replay-import-sources.json
replay-imports: exit 0, 0.087 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpq6odk021/replay-import-sources.json
replay-imports: exit 0, 0.079 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpq6odk021/replay-sources.json
replay-graph: exit 0, 0.163 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpmengkyh5/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpmengkyh5/replay-import-sources.json
replay-imports: exit 0, 0.079 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpmengkyh5/replay-sources.json
replay-graph: exit 0, 0.081 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.081 s
.RUN registry: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';console.log(JSON.stringify(REGISTRY.map(([id])=>id)))
registry: exit 0, 0.079 s
ledger inventory: {'issued': 171, 'active': 152, 'retired': 19}
ledger=171 registry=171 missing=[] extra=[]
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpovtz_r65/replay-import-sources.json
replay-imports: exit 0, 0.186 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpovtz_r65/replay-import-sources.json
replay-imports: exit 0, 0.247 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpovtz_r65/replay-import-sources.json
replay-imports: exit 0, 0.235 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpovtz_r65/replay-import-sources.json
replay-imports: exit 0, 0.203 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpovtz_r65/replay-import-sources.json
replay-imports: exit 0, 0.199 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpovtz_r65/replay-import-sources.json
replay-imports: exit 0, 0.187 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpovtz_r65/replay-sources.json
replay-graph: exit 0, 0.350 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.083 s
.RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpl6k2kyb6/replay-sources.json
replay-graph: exit 0, 0.085 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpncfis4ld/replay-import-sources.json
replay-imports: exit 0, 0.073 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpncfis4ld/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpncfis4ld/replay-sources.json
replay-graph: exit 0, 0.088 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.080 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp3_sblta4/replay-import-sources.json
replay-imports: exit 0, 0.083 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !['loc', 'start', 'end'].includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named and namespace imports only. Any other relative form
// would leave a dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [];
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} '${value}'`);
    }
  }
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp3_sblta4/replay-import-sources.json
replay-imports: exit 0, 0.084 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmp3_sblta4/replay-sources.json
replay-graph: exit 0, 0.081 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.087 s
.
----------------------------------------------------------------------
Ran 48 tests in 11.403s

OK
