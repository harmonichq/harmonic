..RUN pytest: uv run python -m pytest tests/test_empty.py
pytest: exit 5, 0.000 s
.RUN pytest: uv run python -m pytest tests/test_one.py tests/test_two.py
pytest: exit 0, 0.000 s
.RUN case-cache: node $TMPDIR/tmp7po6oe8k/out/case-cache.mjs
case-cache: exit 1, 0.087 s
RUN case-cache: node $TMPDIR/tmppm6ndwty/out/case-cache.mjs
case-cache: exit 1, 0.078 s
.RUN case-cache: node $TMPDIR/tmpx8bfebs_/out/case-cache.mjs
case-cache: exit 0, 1.093 s
............................ledger inventory: {'issued': 171, 'active': 153, 'retired': 18}
ledger inventory: {'issued': 171, 'active': 152, 'retired': 19}
ledger=171 registry=171 missing=[] extra=[]
latest nightly 123: success
nightly success: refreshed 2 PR commit statuses
latest nightly 123: success
nightly success: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
nightly failure: refreshed 2 PR commit statuses
::warning::No scheduled nightly has completed yet; bootstrap passes until the first completed run.
latest nightly 123: success
# executed 130 · failed 0 · deferred 0 · selected 130
# executed 130 · failed 0 · deferred 0 · selected 130
# executed 32 · failed 0 · deferred 0 · selected 32
# executed 33 · failed 0 · deferred 0 · selected 33
# executed 32 · failed 0 · deferred 0 · selected 32
# executed 33 · failed 0 · deferred 0 · selected 33
# executed 20 · failed 0 · deferred 0 · selected 20
# executed 130 · failed 0 · deferred 0 · selected 130
# executed 130 · failed 0 · deferred 0 · selected 130
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpvv1nz2uv/replay-import-sources.json
replay-imports: exit 0, 0.087 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpvv1nz2uv/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpvv1nz2uv/replay-import-sources.json
replay-imports: exit 0, 0.075 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpvv1nz2uv/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpvv1nz2uv/replay-sources.json
replay-graph: exit 0, 0.078 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.082 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpmutf1vjy/replay-import-sources.json
replay-imports: exit 0, 0.080 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpmutf1vjy/replay-import-sources.json
replay-imports: exit 0, 0.072 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpmutf1vjy/replay-import-sources.json
replay-imports: exit 0, 0.078 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpmutf1vjy/replay-sources.json
replay-graph: exit 0, 0.079 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.082 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmps2ai73kk/replay-import-sources.json
replay-imports: exit 0, 0.074 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmps2ai73kk/replay-import-sources.json
replay-imports: exit 0, 0.077 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmps2ai73kk/replay-import-sources.json
replay-imports: exit 0, 0.086 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmps2ai73kk/replay-import-sources.json
replay-imports: exit 0, 0.077 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmps2ai73kk/replay-sources.json
replay-graph: exit 0, 0.078 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpn3tiujvx/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpn3tiujvx/replay-import-sources.json
replay-imports: exit 0, 0.079 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmplvy9euzc/replay-import-sources.json
replay-imports: exit 0, 0.083 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmplvy9euzc/replay-import-sources.json
replay-imports: exit 0, 0.079 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmplvy9euzc/replay-import-sources.json
replay-imports: exit 0, 0.080 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmplvy9euzc/replay-import-sources.json
replay-imports: exit 0, 0.086 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmplvy9euzc/replay-sources.json
replay-graph: exit 0, 0.079 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.077 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpdzdge2vl/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpdzdge2vl/replay-import-sources.json
replay-imports: exit 0, 0.083 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpdzdge2vl/replay-import-sources.json
replay-imports: exit 0, 0.079 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpdzdge2vl/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpdzdge2vl/replay-sources.json
replay-graph: exit 0, 0.079 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.077 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpuwl49udd/replay-import-sources.json
replay-imports: exit 0, 0.075 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpuwl49udd/replay-import-sources.json
replay-imports: exit 0, 0.076 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpuwl49udd/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpuwl49udd/replay-import-sources.json
replay-imports: exit 0, 0.078 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpuwl49udd/replay-sources.json
replay-graph: exit 0, 0.079 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.084 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpd_fqa3fy/replay-import-sources.json
replay-imports: exit 0, 0.082 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpd_fqa3fy/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmps1or3osr/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmps1or3osr/replay-import-sources.json
replay-imports: exit 0, 0.137 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp4rcr8ghv/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp4rcr8ghv/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpptmc9z7c/replay-import-sources.json
replay-imports: exit 0, 0.089 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpptmc9z7c/replay-import-sources.json
replay-imports: exit 0, 0.135 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpo8vg4lxr/replay-import-sources.json
replay-imports: exit 0, 0.132 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpo8vg4lxr/replay-import-sources.json
replay-imports: exit 0, 0.087 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmph_ctx_3j/replay-import-sources.json
replay-imports: exit 0, 0.082 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmph_ctx_3j/replay-import-sources.json
replay-imports: exit 0, 0.088 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp0oqzdnep/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp0oqzdnep/replay-import-sources.json
replay-imports: exit 0, 0.131 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp0oqzdnep/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp0oqzdnep/replay-import-sources.json
replay-imports: exit 0, 0.076 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmp0oqzdnep/replay-sources.json
replay-graph: exit 0, 0.089 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.099 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp60fvk05e/replay-import-sources.json
replay-imports: exit 0, 0.076 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp60fvk05e/replay-import-sources.json
replay-imports: exit 0, 0.083 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp60fvk05e/replay-import-sources.json
replay-imports: exit 0, 0.076 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp60fvk05e/replay-import-sources.json
replay-imports: exit 0, 0.079 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmp60fvk05e/replay-sources.json
replay-graph: exit 0, 0.089 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.138 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpqmgb3uz0/replay-import-sources.json
replay-imports: exit 0, 0.097 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmprt5861hc/replay-import-sources.json
replay-imports: exit 0, 0.084 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp5y4__zl0/replay-import-sources.json
replay-imports: exit 0, 0.079 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp23y_sj3i/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmplmps5af2/replay-import-sources.json
replay-imports: exit 0, 0.088 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpryy0kztw/replay-import-sources.json
replay-imports: exit 0, 0.083 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpjhsf43zc/replay-import-sources.json
replay-imports: exit 0, 0.134 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpzlr_40hk/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpzlr_40hk/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpzlr_40hk/replay-import-sources.json
replay-imports: exit 0, 0.088 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpzlr_40hk/replay-import-sources.json
replay-imports: exit 0, 0.084 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpzlr_40hk/replay-sources.json
replay-graph: exit 0, 0.082 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.136 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpiqwa8qgf/replay-import-sources.json
replay-imports: exit 0, 0.082 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpiqwa8qgf/replay-import-sources.json
replay-imports: exit 0, 0.084 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpiqwa8qgf/replay-sources.json
replay-graph: exit 0, 0.083 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.084 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpa2hgij0c/replay-import-sources.json
replay-imports: exit 0, 0.084 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpa2hgij0c/replay-import-sources.json
replay-imports: exit 0, 0.078 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpa2hgij0c/replay-sources.json
replay-graph: exit 0, 0.074 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.081 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp3fg8072f/replay-import-sources.json
replay-imports: exit 0, 0.087 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp3fg8072f/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmp3fg8072f/replay-sources.json
replay-graph: exit 0, 0.082 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.078 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpyfy7_wle/replay-import-sources.json
replay-imports: exit 0, 0.075 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpyfy7_wle/replay-import-sources.json
replay-imports: exit 0, 0.078 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpyfy7_wle/replay-sources.json
replay-graph: exit 0, 0.078 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.140 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpf9m0zkw0/replay-import-sources.json
replay-imports: exit 0, 0.086 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpf9m0zkw0/replay-import-sources.json
replay-imports: exit 0, 0.083 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpf9m0zkw0/replay-sources.json
replay-graph: exit 0, 0.080 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.135 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpo0yr54qx/replay-import-sources.json
replay-imports: exit 0, 0.092 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpo0yr54qx/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpo0yr54qx/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpo0yr54qx/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpo0yr54qx/replay-sources.json
replay-graph: exit 0, 0.087 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.127 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpnchroqwl/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpnchroqwl/replay-import-sources.json
replay-imports: exit 0, 0.081 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpnchroqwl/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpnchroqwl/replay-import-sources.json
replay-imports: exit 0, 0.087 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpnchroqwl/replay-sources.json
replay-graph: exit 0, 0.085 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.130 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpy4ckr_ep/replay-import-sources.json
replay-imports: exit 0, 0.146 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpy4ckr_ep/replay-import-sources.json
replay-imports: exit 0, 0.075 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpy4ckr_ep/replay-import-sources.json
replay-imports: exit 0, 0.139 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpy4ckr_ep/replay-import-sources.json
replay-imports: exit 0, 0.101 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpy4ckr_ep/replay-sources.json
replay-graph: exit 0, 0.255 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.257 s
.RUN registry: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';console.log(JSON.stringify(REGISTRY.map(([id])=>id)))
registry: exit 0, 0.336 s
ledger inventory: {'issued': 171, 'active': 152, 'retired': 19}
ledger=171 registry=171 missing=[] extra=[]
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp4d9qdr0g/replay-import-sources.json
replay-imports: exit 0, 0.585 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp4d9qdr0g/replay-import-sources.json
replay-imports: exit 0, 0.523 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp4d9qdr0g/replay-import-sources.json
replay-imports: exit 0, 0.493 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp4d9qdr0g/replay-import-sources.json
replay-imports: exit 0, 0.316 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp4d9qdr0g/replay-import-sources.json
replay-imports: exit 0, 0.296 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmp4d9qdr0g/replay-import-sources.json
replay-imports: exit 0, 0.309 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmp4d9qdr0g/replay-sources.json
replay-graph: exit 0, 0.369 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.081 s
.RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmp55jpmx4g/replay-sources.json
replay-graph: exit 0, 0.089 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpklcu28e9/replay-import-sources.json
replay-imports: exit 0, 0.085 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpklcu28e9/replay-import-sources.json
replay-imports: exit 0, 0.087 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpklcu28e9/replay-sources.json
replay-graph: exit 0, 0.080 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.089 s
.RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpa_oc_aq9/replay-import-sources.json
replay-imports: exit 0, 0.078 s
RUN replay-imports: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const files = JSON.parse(readFileSync(process.argv[1], 'utf8'));
const SKIP = ['loc', 'start', 'end', 'extra', 'leadingComments', 'trailingComments', 'innerComments'];
const dynamic = node => node && typeof node === 'object' && (node.type === 'Import'
  || Object.entries(node).some(([key, value]) => !SKIP.includes(key)
    && (Array.isArray(value) ? value.some(dynamic) : dynamic(value))));
// The graph follows named imports only. Any other relative form would leave a
// dependency it cannot map, so it is reported to stop the plan.
const unfollowable = node => {
  if (node.type === 'ExportAllDeclaration') return 'export * from';
  if (node.type === 'ExportNamedDeclaration') return 'export … from';
  if (!node.specifiers.length) return 'side-effect import';
  if (node.specifiers.some(spec => spec.type === 'ImportDefaultSpecifier')) return 'default import';
  if (node.specifiers.some(spec => spec.type === 'ImportNamespaceSpecifier')) return 'namespace import';
  if (node.specifiers.some(spec => spec.type === 'ImportSpecifier'
    && (spec.imported.name ?? spec.imported.value) === 'default')) return '{ default as … } import';
  return null;
};
const literal = node => node?.type === 'StringLiteral' ? node.value
  : node?.type === 'TemplateLiteral' && !node.expressions.length ? node.quasis[0].value.cooked : null;
const metaUrl = node => node?.type === 'MemberExpression' && node.object?.type === 'MetaProperty'
  && node.object.meta.name === 'import' && node.property?.name === 'url';
// A template with expressions reads as a path when its fixed text is
// module-relative or ends in an executable's extension; it cannot be resolved.
const pathlike = node => node.type === 'TemplateLiteral' && node.expressions.length > 0
  && (/^\.\.?\//.test(node.quasis[0].value.cooked) || /\.(py|js|mjs|cjs)$/.test(node.quasis.at(-1).value.cooked));
const result = {};
for (const [file, source] of Object.entries(files)) {
  const targets = new Set(), forms = [], paths = [], computed = [], consumed = new Set();
  const text = node => source.slice(node.start, node.end);
  const program = parse(source, { sourceType: 'module' }).program;
  for (const node of program.body) {
    if (node.source) consumed.add(node.source);
    const value = node.source?.value;
    if (typeof value === 'string' && value.startsWith('.')) {
      targets.add(posix.normalize(posix.join(posix.dirname(file), value)));
      const form = unfollowable(node);
      if (form) forms.push(`${form} not followed: '${value}'`);
    }
  }
  // require() and new URL(…, import.meta.url) resolve their own argument; every
  // other string names a path only as itself. A require of a variable (the
  // environment-named Playwright package) is a computed load it cannot see.
  const scan = node => {
    if (!node || typeof node !== 'object' || consumed.has(node)) return;
    if (Array.isArray(node)) { node.forEach(scan); return; }
    let argument = null, kind = null;
    if (node.type === 'CallExpression' && node.callee?.type === 'Identifier' && node.callee.name === 'require') {
      [argument, kind] = [node.arguments[0], 'require'];
      if (argument && ['Identifier', 'MemberExpression'].includes(argument.type)) argument = null;
    } else if (node.type === 'NewExpression' && node.callee?.name === 'URL' && metaUrl(node.arguments[1])) {
      [argument, kind] = [node.arguments[0], 'url'];
    }
    if (argument) {
      consumed.add(argument);
      const value = literal(argument);
      if (value === null) computed.push(`${kind} ${text(argument)}`); else paths.push([kind, value]);
    }
    const value = literal(node);
    if (value !== null) paths.push(['literal', value]);
    else if (pathlike(node)) computed.push(`template ${text(node)}`);
    for (const [key, child] of Object.entries(node)) if (!SKIP.includes(key)) scan(child);
  };
  scan(program.body);
  result[file] = { targets: [...targets].sort(), dynamic: dynamic(program), forms, paths, computed };
}
console.log(JSON.stringify(result));
 $TMPDIR/tmpa_oc_aq9/replay-import-sources.json
replay-imports: exit 0, 0.088 s
RUN replay-graph: node --input-type=module -e 
import { createRequire } from 'node:module';
import { readFileSync } from 'node:fs';
import { posix } from 'node:path';
const require = createRequire(process.cwd() + '/package.json');
const { parse } = require('@babel/parser');
const snapshots = JSON.parse(readFileSync(process.argv[1], 'utf8'));
function graph(files) {
  const result = {};
  for (const [file, source] of Object.entries(files)) {
    const ast = parse(source, { sourceType: 'module' });
    const nodes = Object.create(null), imports = Object.create(null), shared = [];
    const add = (name, node, comments = []) => {
      nodes[name] = { node, tags: comments.flatMap(c => [...c.value.matchAll(/STORY:([\w-]+:[SR]\d+[a-z]?)/g)].map(m => m[1])) };
    };
    for (const top of ast.program.body) {
      const node = top.type === 'ExportNamedDeclaration' ? top.declaration : top;
      if (node?.type === 'ImportDeclaration') {
        const target = posix.normalize(posix.join(posix.dirname(file), node.source.value));
        if (files[target]) for (const spec of node.specifiers) {
          imports[spec.local.name] = [target, spec.imported?.name || '*'];
          add(spec.local.name, node);
        }
      } else if (node?.type === 'FunctionDeclaration' || node?.type === 'ClassDeclaration') {
        add(node.id.name, node, top.leadingComments || []);
      } else if (node?.type === 'VariableDeclaration') {
        for (const decl of node.declarations) {
          if (decl.id.type !== 'Identifier') { shared.push(source.slice(top.start, top.end)); continue; }
          add(decl.id.name, decl, top.leadingComments || []);
          if (decl.init?.type === 'ObjectExpression') for (const prop of decl.init.properties) {
            const name = prop.key?.name || prop.key?.value;
            if (name) add(`${decl.id.name}.${name}`, prop, prop.leadingComments || []);
          }
        }
      } else shared.push(source.slice(top.start, top.end));
    }
    const full = name => `${file}::${name}`;
    const ref = name => {
      const [first, ...tail] = name.split('.');
      if (imports[first]) {
        const [target, imported] = imports[first];
        return `${target}::${[imported === '*' ? '' : imported, ...tail].filter(Boolean).join('.')}`;
      }
      return nodes[name] ? full(name) : nodes[first] ? full(first) : null;
    };
    function member(node) {
      if (node.type === 'Identifier') return node.name;
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const base = member(node.object);
        const field = node.computed ? node.property.value : node.property.name;
        return base && typeof field === 'string' ? `${base}.${field}` : null;
      }
      return null;
    }
    function references(node, refs, strings, destinations) {
      if (!node || typeof node !== 'object') return;
      if (node.type === 'StringLiteral') strings.add(node.value);
      if (node.type === 'CallExpression' && ['go', 'goto'].includes(node.callee?.name)
          && node.arguments[1]?.type === 'StringLiteral') {
        const destination = node.arguments[1].value;
        if (['diagnose', 'explore', 'changes', 'day'].includes(destination))
          destinations.add(destination === 'explore' ? 'diagnose' : destination);
      }
      if (node.type === 'Identifier') { refs.add(node.name); return; }
      if (['MemberExpression', 'OptionalMemberExpression'].includes(node.type)) {
        const name = member(node);
        if (name) { refs.add(name); return; }
      }
      for (const [key, value] of Object.entries(node)) {
        if (['loc', 'start', 'end', 'leadingComments', 'trailingComments', 'innerComments'].includes(key)) continue;
        if (Array.isArray(value)) value.forEach(child => references(child, refs, strings, destinations));
        else if (value && typeof value === 'object') references(value, refs, strings, destinations);
      }
    }
    result[full('@module')] = { text: shared.join('\n'), deps: [], strings: [], tags: [] };
    for (const [name, { node, tags }] of Object.entries(nodes)) {
      const refs = new Set(), strings = new Set(), destinations = new Set(); references(node, refs, strings, destinations);
      result[full(name)] = { text: source.slice(node.start, node.end) + tags.join(','),
        deps: [...new Set([...refs].map(ref).filter(Boolean)), full('@module')], strings: [...strings], destinations: [...destinations], tags };
    }
  }
  return result;
}
console.log(JSON.stringify(snapshots.map(graph)));
 $TMPDIR/tmpa_oc_aq9/replay-sources.json
replay-graph: exit 0, 0.080 s
RUN story-cases: node --input-type=module -e import {REGISTRY} from './frontend/desk-behavior.replay.mjs';import {storyCase} from './frontend/replay-cases.mjs';console.log(JSON.stringify(Object.fromEntries(REGISTRY.map(([id])=>[id,storyCase(id)]))))
story-cases: exit 0, 0.152 s
.
----------------------------------------------------------------------
Ran 53 tests in 19.723s

OK
