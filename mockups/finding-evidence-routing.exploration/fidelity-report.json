{
 "viewport": {
  "width": 1440,
  "height": 900
 },
 "dark": {
  "computedStyleDiff": {
   "checked": 84,
   "matched": 84,
   "absentInApp": [
    ".qrow[data-tier=\"priced\"]",
    ".qrow[data-tier=\"priced\"] .lab",
    ".qrow[data-tier=\"priced\"] .tag",
    ".qrow[data-tier=\"priced\"] .den",
    ".qrow[data-tier=\"priced\"] .den .v",
    ".dw-canvas > .body"
   ],
   "mismatches": [],
   "expected": [
    ".ec-key-item row-gap: app \"1px\" vs mock \"6px\" — FINDING 3: one line has no row gap",
    ".ec-key-item display: app \"grid\" vs mock \"flex\" — FINDING 3: a legend chip is ONE LINE. The shipped item is an `inline-grid` that drops its detail under the label, which at three cohorts in a 430px canvas produced three wrapped paragraphs of engine caveat at data weight under the data — the thing DESIGN.md rule 5 forbids by name.",
    ".ec-key-item grid-template-columns: app \"22px 264px\" vs mock \"22px auto\" — FINDING 3: ditto — no grid, so no tracks",
    ".ec-key-item grid-template-rows: app \"13px 22px\" vs mock \"none\" — FINDING 3: ditto — the detail is on the label's own line now",
    ".ec-key-item align-items: app \"center\" vs mock \"baseline\" — FINDING 3: baseline, so the mark, the name and the count sit on one line",
    ".inspector grid-template-rows: app \"30px 661px 98px\" vs mock \"30px 731px 28px\" — round 5 block 8: the dock is no longer a 98px reserve, so the level track takes that height. This is the term-48 re-settle, in one number.",
    ".inspector > .body grid-template-rows: app \"32px 629px\" vs mock \"32px 438.172px 260.828px\" — ditto — the level track grows; the crumb row differs by 1px because the count accessory sits in it. ROUND 6, FORM 1 adds a THIRD track: the factor dropdown's rest line, which is a row of the column above the level rather than content inside it.",
    ".crumb .trail button color: app \"color(srgb 0.960784 0.92549 0.878431 / 0.55)\" vs mock \"rgb(163, 150, 138)\" — F7: the crumb root is `--mk-text` at 55%, 3.56:1 in light. It is the column's one way back, so it takes `--mk-muted` — the designed meta ink, 6.21:1 light / 5.47:1 dark — rather than the dim register: a navigation control should not be reading at the floor.",
    ".crumb .trail button border-top-color: app \"color(srgb 0.960784 0.92549 0.878431 / 0.55)\" vs mock \"rgb(163, 150, 138)\" — F7: not a border. .crumb .trail button paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.crumb .trail button|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".crumb .trail button border-bottom-color: app \"color(srgb 0.960784 0.92549 0.878431 / 0.55)\" vs mock \"rgb(163, 150, 138)\" — F7: not a border. .crumb .trail button paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.crumb .trail button|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".crumb .trail .chev color: app \"color(srgb 0.960784 0.92549 0.878431 / 0.35)\" vs mock \"color(srgb 0.960784 0.92549 0.878431 / 0.62)\" — F7: the crumb separator is `--mk-text` at 35% — 2.10:1 light / 2.90:1 dark, the worst text pair on the surface and failing in both themes. Same dim-meta ink.",
    ".crumb .trail .chev border-top-color: app \"color(srgb 0.960784 0.92549 0.878431 / 0.35)\" vs mock \"color(srgb 0.960784 0.92549 0.878431 / 0.62)\" — F7: not a border. .crumb .trail .chev paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.crumb .trail .chev|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".crumb .trail .chev border-bottom-color: app \"color(srgb 0.960784 0.92549 0.878431 / 0.35)\" vs mock \"color(srgb 0.960784 0.92549 0.878431 / 0.62)\" — F7: not a border. .crumb .trail .chev paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.crumb .trail .chev|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".level .inner padding-bottom: app \"8px\" vs mock \"14px\" — item 12: the judgment block closes with air before the claim selector, which item 4 moved outside `.inner`",
    ".lvl-cap margin-top: app \"0px\" vs mock \"6px\" — item 12: the Occurrences cap takes air above it, so the table reads as its own section rather than as more of the judgment block",
    ".lvl-cap .meta margin-left: app \"64.4062px\" vs mock \"66.8438px\" — an AUTO margin, resolved against a different string length — the rule is identical",
    ".qrow .go color: app \"rgb(224, 127, 63)\" vs mock \"rgb(239, 148, 89)\" — F7: ditto — the queue row's chevron is the same `--ck-accent` at the same 3.95:1.",
    ".qrow .go border-top-color: app \"rgb(224, 127, 63)\" vs mock \"rgb(239, 148, 89)\" — F7: not a border. .qrow .go paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.qrow .go|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".qrow .go border-bottom-color: app \"rgb(224, 127, 63)\" vs mock \"rgb(239, 148, 89)\" — F7: not a border. .qrow .go paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.qrow .go|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".linkbtn font-weight: app \"400\" vs mock \"500\" — FINDING 14: the routes inherit `.fer-open`'s weight as well as its ink, so the two kinds of route in this column read as one kind of thing",
    ".linkbtn color: app \"rgb(224, 127, 63)\" vs mock \"rgb(239, 148, 89)\" — FINDING 14 + F7: the coincidence routes inherit `Open case file ›`'s treatment. The shipped `.linkbtn` is `--ck-accent` underlined, which is a website tell inside an app that navigates without a single underline anywhere else; the amendment also reserved the accent + right-alignment for the ONE action in this column, and two more of them turned a deliberate accent into noise. F7 moves the ink again, to `--primary-600`: `--ck-accent` on bone is 3.95:1 as text, so every accent-inked string on this surface failed AA in light at once. The accent's own next step clears it in both themes (5.78:1 light, 6.79:1 dark) without introducing a colour the app does not already pair.",
    ".linkbtn border-top-color: app \"rgb(224, 127, 63)\" vs mock \"rgb(239, 148, 89)\" — F7: not a border. .linkbtn paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.linkbtn|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".linkbtn border-bottom-color: app \"rgb(224, 127, 63)\" vs mock \"rgb(239, 148, 89)\" — F7: not a border. .linkbtn paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.linkbtn|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".linkbtn padding-top: app \"0px\" vs mock \"6px\" — F8: the painted line is 14px tall and two of these stack 3px apart, so neither the target nor the spacing met WCAG 2.2 AA's 24x24. The padding takes each to 24px and sets the pitch to 27px, which a centred overlay of the same height could not have done without the two boxes overlapping and stealing each other's clicks.",
    ".linkbtn padding-bottom: app \"0px\" vs mock \"6px\" — F8: ditto — the other half of the same padding",
    ".ev-row .arrow color: app \"color(srgb 0.639216 0.588235 0.541176 / 0.7)\" vs mock \"color(srgb 0.960784 0.92549 0.878431 / 0.62)\" — F7: the production table's `→` is `--mk-muted` at 70%, which measures 3.21:1 light and 3.42:1 dark — it fails in BOTH themes, which is why it is not fixed by a light-only override. It takes this surface's one dim-meta ink instead.",
    ".ev-row .arrow border-top-color: app \"color(srgb 0.639216 0.588235 0.541176 / 0.7)\" vs mock \"color(srgb 0.960784 0.92549 0.878431 / 0.62)\" — F7: not a border. .ev-row .arrow paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.ev-row .arrow|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".ev-row .arrow border-bottom-color: app \"color(srgb 0.639216 0.588235 0.541176 / 0.7)\" vs mock \"color(srgb 0.960784 0.92549 0.878431 / 0.62)\" — F7: not a border. .ev-row .arrow paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.ev-row .arrow|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".more color: app \"rgb(224, 127, 63)\" vs mock \"rgb(239, 148, 89)\" — F7: the expander is `--ck-accent`, which on bone measures 3.95:1 as text — under DESIGN.md line 188's own 4.5:1 bar. It takes the surface's route ink (`--primary-600`, 5.78:1 light / 6.79:1 dark), which is the same step the two in-body routes take, so every accent-inked string in this column reads at one value.",
    ".more border-top-color: app \"rgb(224, 127, 63)\" vs mock \"rgb(239, 148, 89)\" — F7: not a border. .more paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.more|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".watch border-top-width: app \"0px\" vs mock \"1px\" — block 8: the hairline that replaces the fill",
    ".watch border-top-color: app \"rgb(245, 236, 224)\" vs mock \"rgb(50, 46, 41)\" — block 8: the app declares no top border at all, so its \"colour\" is the inherited text ink of a 0px edge — the mock's is `--ck-hair`, the pane's own hairline",
    ".watch padding-top: app \"10px\" vs mock \"7px\" — block 8: padding sized to one line",
    ".watch padding-bottom: app \"11px\" vs mock \"7px\" — block 8: padding sized to one line",
    ".watch display: app \"grid\" vs mock \"block\" — block 8: one line, so no two-column grid",
    ".watch grid-template-columns: app \"393px 0px\" vs mock \"minmax(0px, 1fr) auto\" — block 8: no grid",
    ".watch grid-template-rows: app \"13px 16px 15px\" vs mock \"none\" — block 8: the three shipped ranks (kind / what / how) are one line now, so there are no rows to lay out",
    ".watch min-height: app \"98px\" vs mock \"0px\" — block 8: the dock is one line at the column's true bottom, not a 98px reserve (re-settles migrated lock term 48)",
    ".canvas-head .head-rest .meta margin-left: app \"367.016px\" vs mock \"0px\" — an AUTO margin, resolved against a different string length — the two fixtures pool different reading counts, so \"window N of M readings\" is not the same width. The rule is identical; same class as the `.lvl-cap .meta` entry above.",
    ".lane-stack grid-template-rows: app \"11px 11px\" vs mock \"11px\" — workstream A, omission (b): the shipped stack carries a second row of I:C block verdicts beneath the basal lane. The #31 ruling names the BASAL verdict lane, and with one lane the key's lead-word grammar (which exists to tell the two rows apart) is doing nothing. One row here, two in the app — a real divergence, not a resolution artifact.",
    ".lane background-color: app \"rgb(26, 22, 17)\" vs mock \"color(srgb 0.213961 0.196706 0.185725)\" — F5: the OTHER half of the same pair, and the half round 9 missed. `.lane`'s own ground was still `--ck-inset` — a black slab under the chart on bone (14.51:1 against the pane) and a strip that vanished into the pane on the desk (1.03:1). The strip's `gap: 1px` means the strip IS the boundary between the 48 cells, so this is a 1.4.11 mark and not a matter of taste. It is a recessive track in both themes now: 1.16:1 light / 1.47:1 dark against the pane it sits on.",
    ".lane button background-color: app \"rgba(0, 0, 0, 0)\" vs mock \"color(srgb 0.619843 0.592784 0.562196)\" — FINDING 10 + F5: `--ck-inset` is a near-black in BOTH themes, so the strip was the loudest ink on a bone page and nearly invisible on the dark desk — one fill that never got its light/dark pass, under the least consequential element on the canvas. Round 9 re-tuned the CELL and left the strip, which inverted the pair instead of fixing it: 12.19:1 cell-against-strip in light and 1.37:1 in dark, at which no reader can see the 48 cell boundaries at all. One declaration now, derived from the column's own ink and surface, measured at 3.33:1 light / 4.39:1 dark against the strip below."
   ],
   "contentResolved": [
    ".pane grid-template-rows: app \"30px 759px\" vs mock \"38px 751px\"",
    ".pane > header grid-template-rows: app \"21px\" vs mock \"29px\"",
    ".canvas-pane > header.canvas-head grid-template-rows: app \"21px\" vs mock \"29px\"",
    ".canvas-head .head-swap grid-template-rows: app \"14px\" vs mock \"29px\"",
    ".ec-chart-key grid-template-columns: app \"292px 292px 292px 0px 0px\" vs mock \"291.656px 291.672px 291.656px 0px 0px\"",
    ".ec-chart-key grid-template-rows: app \"36px\" vs mock \"31px\""
   ]
  },
  "chartOptionDiff": [
   "tooltip: app [{\"trigger\":\"axis\",\"showContent\":false,\"axisPointer\":{\"type\":\"cross\",\"label\":{\"show\":false},\"axis\":\"auto\",\"animation\":\"auto\",\"animationDurationUpdate\":200,\"animationEasingUpdate\":\"exponentialOut\",\"crossStyle\":{\"color\":\"#999\",\"width\":1,\"type\":\"dashed\",\"textStyle\":{}}},\"z\":60,\"show\":true,\"triggerOn\":\"mousemove|click\",\"alwaysShowContent\":false,\"displayMode\":\"single\",\"renderMode\":\"auto\",\"confine\":null,\"showDelay\":0,\"hideDelay\":100,\"transitionDuration\":0.4,\"enterable\":false,\"backgroundColor\":\"#fff\",\"shadowBlur\":10,\"shadowColor\":\"rgba(0, 0, 0, .2)\",\"shadowOffsetX\":1,\"shadowOffsetY\":2,\"borderRadius\":4,\"borderWidth\":1,\"padding\":null,\"extraCssText\":\"\",\"textStyle\":{\"color\":\"#666\",\"fontSize\":14}}] vs mock [{\"trigger\":\"axis\",\"showContent\":false,\"axisPointer\":{\"type\":\"cross\",\"crossStyle\":{\"color\":\"#a3968a\",\"width\":1,\"type\":\"dashed\",\"opacity\":0.7,\"textStyle\":{}},\"lineStyle\":{\"color\":\"#a3968a\",\"width\":1,\"type\":\"dashed\",\"opacity\":0.7},\"label\":{\"show\":true,\"backgroundColor\":\"#262220\",\"borderColor\":\"#322e29\",\"borderWidth\":1,\"color\":\"#f5ece0\",\"fontFamily\":\"ui-monospace, \\\"SF Mono\\\", \\\"JetBrains Mono\\\", Menlo, monospace\",\"fontSize\":10,\"padding\":[3,6]},\"axis\":\"auto\",\"animation\":\"auto\",\"animationDurationUpdate\":200,\"animationEasingUpdate\":\"exponentialOut\"},\"z\":60,\"show\":true,\"triggerOn\":\"mousemove|click\",\"alwaysShowContent\":false,\"displayMode\":\"single\",\"renderMode\":\"auto\",\"confine\":null,\"showDelay\":0,\"hideDelay\":100,\"transitionDuration\":0.4,\"enterable\":false,\"backgroundColor\":\"#fff\",\"shadowBlur\":10,\"shadowColor\":\"rgba(0, 0, 0, .2)\",\"shadowOffsetX\":1,\"shadowOffsetY\":2,\"borderRadius\":4,\"borderWidth\":1,\"padding\":null,\"extraCssText\":\"\",\"textStyle\":{\"color\":\"#666\",\"fontSize\":14}}]",
   "yAxis.max: app 300 vs mock 240",
   "yAxis.name: app \"mg/dL\" vs mock \"\"",
   "yAxis.nameGap: app 8 vs mock 15",
   "yAxis.nameTextStyle.color: app \"#a3968a\" vs mock undefined",
   "yAxis.nameTextStyle.fontFamily: app \"Inter\" vs mock undefined",
   "yAxis.nameTextStyle.fontSize: app 10 vs mock undefined",
   "yAxis.nameTextStyle.align: app \"left\" vs mock undefined",
   "series[near_rule:line:limited].lineStyle.color: app \"#c98a4e\" vs mock \"#d59a60\"",
   "series[near_rule:line:limited].itemStyle.borderColor: app \"#c98a4e\" vs mock \"#d59a60\""
  ],
  "pooledOptionDiff": [],
  "chartOptionExpected": [
   "aria.description — FINDING 9: ditto — the description is built from the same labels",
   "aria.label.description — FINDING 9: ditto. ECharts normalises the description onto `aria.label` as well, so the one string differs at two paths and both are named rather than one being left to read as drift.",
   "yAxis.interval — FINDING 5: one ladder across both projections. 40 rather than the shipped 60 so that the max lands ON the interval and — the load-bearing half — so neither 70 nor 180 falls on the value ladder, which is what lets the two thresholds be the target band's own edges instead of ticks in identical type.",
   "series[fired:spread:supported].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[fired:line:supported].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[fired:spread:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[fired:line:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[near_rule:spread:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[near_rule:line:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[neutral:spread:supported].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[neutral:line:supported].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[neutral:spread:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[neutral:line:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[Target range].markArea.itemStyle.color — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.show — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.position — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.color — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.fontSize — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.fontFamily — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one."
  ],
  "pooledOptionExpected": [
   "legend — FINDING 5: ECharts' own legend floated over the plot area. Its keys are read back off this same option and drawn as chips in the canvas header rail beside the title, where the window meta already lives, so the plot carries data and the head carries the explanation.",
   "yAxis[0].max — FINDING 5: 240, not 300. The shipped domain left the top 40% of the largest element on the page rendering nothing; 240 is the first multiple of 40 above the highest value anything on either projection draws (226), so the max sits ON the ladder and roughly 7% is empty above the data.",
   "yAxis[0].interval — FINDING 5: 40, not 60 — one ladder shared with the lens, and the interval that keeps 70 and 180 off it. See the `yAxis.interval` entry in EXPECTED_OPTION.",
   "series[8 Occurrences].itemStyle.color — FINDING 18: one token pair for the two dot states. The shipped scatter draws at `--primary-600`, which on bone is a near-full-contrast dark teal — so unselected dots were LOUDER in light than the accent marking the ones being read, and the emphasis hierarchy inverted between themes. `--fer-dot` is the recessive half, tuned per theme against its own ground."
  ],
  "geometry": {
   "pageScrollX": 0,
   "pageScrollY": 0,
   "levelScrollsInternally": false,
   "breadcrumbClipped": false,
   "unresolvedTokens": []
  }
 },
 "hover": {
  "selectedRows": 0,
  "rowsCarryingATitleAttribute": 0
 },
 "windowUnchanged": {
  "unchanged": true,
  "before": {
   "windowFrom": "00:00",
   "windowTo": "23:45",
   "windowLabel": "24 H 00:00–24:00{sp|  ·  25–75 spread 16 mg/dL}",
   "parkedLabel": null,
   "xExtent": [
    "00:00",
    "23:45"
   ],
   "xBins": 96
  },
  "after": {
   "windowFrom": "00:00",
   "windowTo": "23:45",
   "windowLabel": "24 H 00:00–24:00{sp|  ·  25–75 spread 16 mg/dL}",
   "parkedLabel": null,
   "xExtent": [
    "00:00",
    "23:45"
   ],
   "xBins": 96
  }
 },
 "highlightedRow": "lows-synthetic-3",
 "selected": {
  "selectedRows": 1
 },
 "populationDefaultFrame": "over_treated_low",
 "populationHighlightedRow": "lows-synthetic-2",
 "verdictBand": {
  "segments": [
   "fired",
   "near_rule",
   "neutral"
  ],
  "pressed": "fired",
  "rosterRows": 5,
  "dotsDrawn": 17
 },
 "verdictBandDrilled": {
  "pressed": "near_rule",
  "rosterRows": 4,
  "dotsDrawn": 17
 },
 "populationExpanderPresent": true,
 "light": {
  "computedStyleDiff": {
   "checked": 84,
   "matched": 84,
   "absentInApp": [
    ".qrow[data-tier=\"priced\"]",
    ".qrow[data-tier=\"priced\"] .lab",
    ".qrow[data-tier=\"priced\"] .tag",
    ".qrow[data-tier=\"priced\"] .den",
    ".qrow[data-tier=\"priced\"] .den .v",
    ".dw-canvas > .body"
   ],
   "mismatches": [],
   "expected": [
    ".ec-key-item row-gap: app \"1px\" vs mock \"6px\" — FINDING 3: one line has no row gap",
    ".ec-key-item display: app \"grid\" vs mock \"flex\" — FINDING 3: a legend chip is ONE LINE. The shipped item is an `inline-grid` that drops its detail under the label, which at three cohorts in a 430px canvas produced three wrapped paragraphs of engine caveat at data weight under the data — the thing DESIGN.md rule 5 forbids by name.",
    ".ec-key-item grid-template-columns: app \"22px 264px\" vs mock \"22px auto\" — FINDING 3: ditto — no grid, so no tracks",
    ".ec-key-item grid-template-rows: app \"13px 22px\" vs mock \"none\" — FINDING 3: ditto — the detail is on the label's own line now",
    ".ec-key-item align-items: app \"center\" vs mock \"baseline\" — FINDING 3: baseline, so the mark, the name and the count sit on one line",
    ".inspector grid-template-rows: app \"30px 661px 98px\" vs mock \"30px 731px 28px\" — round 5 block 8: the dock is no longer a 98px reserve, so the level track takes that height. This is the term-48 re-settle, in one number.",
    ".inspector > .body grid-template-rows: app \"32px 629px\" vs mock \"32px 438.172px 260.828px\" — ditto — the level track grows; the crumb row differs by 1px because the count accessory sits in it. ROUND 6, FORM 1 adds a THIRD track: the factor dropdown's rest line, which is a row of the column above the level rather than content inside it.",
    ".crumb .trail button color: app \"color(srgb 0.0901961 0.133333 0.0980392 / 0.55)\" vs mock \"rgb(78, 88, 79)\" — F7: the crumb root is `--mk-text` at 55%, 3.56:1 in light. It is the column's one way back, so it takes `--mk-muted` — the designed meta ink, 6.21:1 light / 5.47:1 dark — rather than the dim register: a navigation control should not be reading at the floor.",
    ".crumb .trail button border-top-color: app \"color(srgb 0.0901961 0.133333 0.0980392 / 0.55)\" vs mock \"rgb(78, 88, 79)\" — F7: not a border. .crumb .trail button paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.crumb .trail button|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".crumb .trail button border-bottom-color: app \"color(srgb 0.0901961 0.133333 0.0980392 / 0.55)\" vs mock \"rgb(78, 88, 79)\" — F7: not a border. .crumb .trail button paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.crumb .trail button|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".crumb .trail .chev color: app \"color(srgb 0.0901961 0.133333 0.0980392 / 0.35)\" vs mock \"color(srgb 0.0901961 0.133333 0.0980392 / 0.66)\" — F7: the crumb separator is `--mk-text` at 35% — 2.10:1 light / 2.90:1 dark, the worst text pair on the surface and failing in both themes. Same dim-meta ink.",
    ".crumb .trail .chev border-top-color: app \"color(srgb 0.0901961 0.133333 0.0980392 / 0.35)\" vs mock \"color(srgb 0.0901961 0.133333 0.0980392 / 0.66)\" — F7: not a border. .crumb .trail .chev paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.crumb .trail .chev|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".crumb .trail .chev border-bottom-color: app \"color(srgb 0.0901961 0.133333 0.0980392 / 0.35)\" vs mock \"color(srgb 0.0901961 0.133333 0.0980392 / 0.66)\" — F7: not a border. .crumb .trail .chev paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.crumb .trail .chev|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".level .inner padding-bottom: app \"8px\" vs mock \"14px\" — item 12: the judgment block closes with air before the claim selector, which item 4 moved outside `.inner`",
    ".lvl-cap margin-top: app \"0px\" vs mock \"6px\" — item 12: the Occurrences cap takes air above it, so the table reads as its own section rather than as more of the judgment block",
    ".lvl-cap .meta margin-left: app \"64.4062px\" vs mock \"66.8438px\" — an AUTO margin, resolved against a different string length — the rule is identical",
    ".qrow .go color: app \"rgb(179, 91, 46)\" vs mock \"rgb(143, 69, 36)\" — F7: ditto — the queue row's chevron is the same `--ck-accent` at the same 3.95:1.",
    ".qrow .go border-top-color: app \"rgb(179, 91, 46)\" vs mock \"rgb(143, 69, 36)\" — F7: not a border. .qrow .go paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.qrow .go|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".qrow .go border-bottom-color: app \"rgb(179, 91, 46)\" vs mock \"rgb(143, 69, 36)\" — F7: not a border. .qrow .go paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.qrow .go|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".linkbtn font-weight: app \"400\" vs mock \"500\" — FINDING 14: the routes inherit `.fer-open`'s weight as well as its ink, so the two kinds of route in this column read as one kind of thing",
    ".linkbtn color: app \"rgb(179, 91, 46)\" vs mock \"rgb(143, 69, 36)\" — FINDING 14 + F7: the coincidence routes inherit `Open case file ›`'s treatment. The shipped `.linkbtn` is `--ck-accent` underlined, which is a website tell inside an app that navigates without a single underline anywhere else; the amendment also reserved the accent + right-alignment for the ONE action in this column, and two more of them turned a deliberate accent into noise. F7 moves the ink again, to `--primary-600`: `--ck-accent` on bone is 3.95:1 as text, so every accent-inked string on this surface failed AA in light at once. The accent's own next step clears it in both themes (5.78:1 light, 6.79:1 dark) without introducing a colour the app does not already pair.",
    ".linkbtn border-top-color: app \"rgb(179, 91, 46)\" vs mock \"rgb(143, 69, 36)\" — F7: not a border. .linkbtn paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.linkbtn|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".linkbtn border-bottom-color: app \"rgb(179, 91, 46)\" vs mock \"rgb(143, 69, 36)\" — F7: not a border. .linkbtn paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.linkbtn|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".linkbtn padding-top: app \"0px\" vs mock \"6px\" — F8: the painted line is 14px tall and two of these stack 3px apart, so neither the target nor the spacing met WCAG 2.2 AA's 24x24. The padding takes each to 24px and sets the pitch to 27px, which a centred overlay of the same height could not have done without the two boxes overlapping and stealing each other's clicks.",
    ".linkbtn padding-bottom: app \"0px\" vs mock \"6px\" — F8: ditto — the other half of the same padding",
    ".ev-row .arrow color: app \"color(srgb 0.305882 0.345098 0.309804 / 0.7)\" vs mock \"color(srgb 0.0901961 0.133333 0.0980392 / 0.66)\" — F7: the production table's `→` is `--mk-muted` at 70%, which measures 3.21:1 light and 3.42:1 dark — it fails in BOTH themes, which is why it is not fixed by a light-only override. It takes this surface's one dim-meta ink instead.",
    ".ev-row .arrow border-top-color: app \"color(srgb 0.305882 0.345098 0.309804 / 0.7)\" vs mock \"color(srgb 0.0901961 0.133333 0.0980392 / 0.66)\" — F7: not a border. .ev-row .arrow paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.ev-row .arrow|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".ev-row .arrow border-bottom-color: app \"color(srgb 0.305882 0.345098 0.309804 / 0.7)\" vs mock \"color(srgb 0.0901961 0.133333 0.0980392 / 0.66)\" — F7: not a border. .ev-row .arrow paints no bottom edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.ev-row .arrow|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".more color: app \"rgb(179, 91, 46)\" vs mock \"rgb(143, 69, 36)\" — F7: the expander is `--ck-accent`, which on bone measures 3.95:1 as text — under DESIGN.md line 188's own 4.5:1 bar. It takes the surface's route ink (`--primary-600`, 5.78:1 light / 6.79:1 dark), which is the same step the two in-body routes take, so every accent-inked string in this column reads at one value.",
    ".more border-top-color: app \"rgb(179, 91, 46)\" vs mock \"rgb(143, 69, 36)\" — F7: not a border. .more paints no top edge here, so `currentColor` reports the element's own text ink a second time — the ink named in the `.more|color` entry above, and the same value. Same artifact the `.watch|border-top-color` entry names.",
    ".watch background-color: app \"rgb(227, 215, 197)\" vs mock \"rgba(0, 0, 0, 0)\" — block 8: unfilled — the hairline above it is what separates it now",
    ".watch border-top-width: app \"0px\" vs mock \"1px\" — block 8: the hairline that replaces the fill",
    ".watch border-top-color: app \"rgb(23, 34, 25)\" vs mock \"rgb(222, 210, 189)\" — block 8: the app declares no top border at all, so its \"colour\" is the inherited text ink of a 0px edge — the mock's is `--ck-hair`, the pane's own hairline",
    ".watch padding-top: app \"10px\" vs mock \"7px\" — block 8: padding sized to one line",
    ".watch padding-bottom: app \"11px\" vs mock \"7px\" — block 8: padding sized to one line",
    ".watch display: app \"grid\" vs mock \"block\" — block 8: one line, so no two-column grid",
    ".watch grid-template-columns: app \"393px 0px\" vs mock \"minmax(0px, 1fr) auto\" — block 8: no grid",
    ".watch grid-template-rows: app \"13px 16px 15px\" vs mock \"none\" — block 8: the three shipped ranks (kind / what / how) are one line now, so there are no rows to lay out",
    ".watch min-height: app \"98px\" vs mock \"0px\" — block 8: the dock is one line at the column's true bottom, not a 98px reserve (re-settles migrated lock term 48)",
    ".canvas-head .head-rest .meta margin-left: app \"367.016px\" vs mock \"0px\" — an AUTO margin, resolved against a different string length — the two fixtures pool different reading counts, so \"window N of M readings\" is not the same width. The rule is identical; same class as the `.lvl-cap .meta` entry above.",
    ".lane-stack grid-template-rows: app \"11px 11px\" vs mock \"11px\" — workstream A, omission (b): the shipped stack carries a second row of I:C block verdicts beneath the basal lane. The #31 ruling names the BASAL verdict lane, and with one lane the key's lead-word grammar (which exists to tell the two rows apart) is doing nothing. One row here, two in the app — a real divergence, not a resolution artifact.",
    ".lane background-color: app \"color(srgb 0.0739608 0.112549 0.0932549)\" vs mock \"color(srgb 0.883922 0.854902 0.797961)\" — F5: the OTHER half of the same pair, and the half round 9 missed. `.lane`'s own ground was still `--ck-inset` — a black slab under the chart on bone (14.51:1 against the pane) and a strip that vanished into the pane on the desk (1.03:1). The strip's `gap: 1px` means the strip IS the boundary between the 48 cells, so this is a 1.4.11 mark and not a matter of taste. It is a recessive track in both themes now: 1.16:1 light / 1.47:1 dark against the pane it sits on.",
    ".lane button background-color: app \"rgba(0, 0, 0, 0)\" vs mock \"color(srgb 0.452549 0.462745 0.417569)\" — FINDING 10 + F5: `--ck-inset` is a near-black in BOTH themes, so the strip was the loudest ink on a bone page and nearly invisible on the dark desk — one fill that never got its light/dark pass, under the least consequential element on the canvas. Round 9 re-tuned the CELL and left the strip, which inverted the pair instead of fixing it: 12.19:1 cell-against-strip in light and 1.37:1 in dark, at which no reader can see the 48 cell boundaries at all. One declaration now, derived from the column's own ink and surface, measured at 3.33:1 light / 4.39:1 dark against the strip below."
   ],
   "contentResolved": [
    ".pane grid-template-rows: app \"30px 759px\" vs mock \"38px 751px\"",
    ".pane > header grid-template-rows: app \"21px\" vs mock \"29px\"",
    ".canvas-pane > header.canvas-head grid-template-rows: app \"21px\" vs mock \"29px\"",
    ".canvas-head .head-swap grid-template-rows: app \"14px\" vs mock \"29px\"",
    ".ec-chart-key grid-template-columns: app \"292px 292px 292px 0px 0px\" vs mock \"291.656px 291.672px 291.656px 0px 0px\"",
    ".ec-chart-key grid-template-rows: app \"36px\" vs mock \"31px\""
   ]
  },
  "chartOptionDiff": [
   "tooltip: app [{\"trigger\":\"axis\",\"showContent\":false,\"axisPointer\":{\"type\":\"cross\",\"label\":{\"show\":false},\"axis\":\"auto\",\"animation\":\"auto\",\"animationDurationUpdate\":200,\"animationEasingUpdate\":\"exponentialOut\",\"crossStyle\":{\"color\":\"#999\",\"width\":1,\"type\":\"dashed\",\"textStyle\":{}}},\"z\":60,\"show\":true,\"triggerOn\":\"mousemove|click\",\"alwaysShowContent\":false,\"displayMode\":\"single\",\"renderMode\":\"auto\",\"confine\":null,\"showDelay\":0,\"hideDelay\":100,\"transitionDuration\":0.4,\"enterable\":false,\"backgroundColor\":\"#fff\",\"shadowBlur\":10,\"shadowColor\":\"rgba(0, 0, 0, .2)\",\"shadowOffsetX\":1,\"shadowOffsetY\":2,\"borderRadius\":4,\"borderWidth\":1,\"padding\":null,\"extraCssText\":\"\",\"textStyle\":{\"color\":\"#666\",\"fontSize\":14}}] vs mock [{\"trigger\":\"axis\",\"showContent\":false,\"axisPointer\":{\"type\":\"cross\",\"crossStyle\":{\"color\":\"#4e584f\",\"width\":1,\"type\":\"dashed\",\"opacity\":0.7,\"textStyle\":{}},\"lineStyle\":{\"color\":\"#4e584f\",\"width\":1,\"type\":\"dashed\",\"opacity\":0.7},\"label\":{\"show\":true,\"backgroundColor\":\"#f3eadb\",\"borderColor\":\"#ded2bd\",\"borderWidth\":1,\"color\":\"#172219\",\"fontFamily\":\"ui-monospace, \\\"SF Mono\\\", \\\"JetBrains Mono\\\", Menlo, monospace\",\"fontSize\":10,\"padding\":[3,6]},\"axis\":\"auto\",\"animation\":\"auto\",\"animationDurationUpdate\":200,\"animationEasingUpdate\":\"exponentialOut\"},\"z\":60,\"show\":true,\"triggerOn\":\"mousemove|click\",\"alwaysShowContent\":false,\"displayMode\":\"single\",\"renderMode\":\"auto\",\"confine\":null,\"showDelay\":0,\"hideDelay\":100,\"transitionDuration\":0.4,\"enterable\":false,\"backgroundColor\":\"#fff\",\"shadowBlur\":10,\"shadowColor\":\"rgba(0, 0, 0, .2)\",\"shadowOffsetX\":1,\"shadowOffsetY\":2,\"borderRadius\":4,\"borderWidth\":1,\"padding\":null,\"extraCssText\":\"\",\"textStyle\":{\"color\":\"#666\",\"fontSize\":14}}]",
   "yAxis.max: app 300 vs mock 240",
   "yAxis.name: app \"mg/dL\" vs mock \"\"",
   "yAxis.nameGap: app 8 vs mock 15",
   "yAxis.nameTextStyle.color: app \"#4e584f\" vs mock undefined",
   "yAxis.nameTextStyle.fontFamily: app \"Inter\" vs mock undefined",
   "yAxis.nameTextStyle.fontSize: app 10 vs mock undefined",
   "yAxis.nameTextStyle.align: app \"left\" vs mock undefined",
   "series[near_rule:line:limited].lineStyle.color: app \"#9b562d\" vs mock \"#5d2a0c\"",
   "series[near_rule:line:limited].itemStyle.borderColor: app \"#9b562d\" vs mock \"#5d2a0c\""
  ],
  "pooledOptionDiff": [],
  "chartOptionExpected": [
   "aria.description — FINDING 9: ditto — the description is built from the same labels",
   "aria.label.description — FINDING 9: ditto. ECharts normalises the description onto `aria.label` as well, so the one string differs at two paths and both are named rather than one being left to read as drift.",
   "yAxis.interval — FINDING 5: one ladder across both projections. 40 rather than the shipped 60 so that the max lands ON the interval and — the load-bearing half — so neither 70 nor 180 falls on the value ladder, which is what lets the two thresholds be the target band's own edges instead of ticks in identical type.",
   "series[fired:spread:supported].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[fired:line:supported].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[fired:spread:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[fired:line:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[near_rule:spread:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[near_rule:line:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[neutral:spread:supported].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[neutral:line:supported].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[neutral:spread:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[neutral:line:limited].name — FINDING 9: one noun per concept. A series `name` is composed from the cohort label, and the labels take the settled partition words (`Meets criteria` / `Borderline` / `Does not meet`). The name reaches the reader through the chart's aria description, so leaving it on `Rule matched` would have put the retired vocabulary in the reader's ear beside a band that had moved on. A SUFFIX key, so it reaches every series' name and nothing else on those series — the round-4 light-theme Near-rule ink deviation sits on the same objects and stays in the count where it belongs.",
   "series[Target range].markArea.itemStyle.color — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.show — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.position — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.color — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.fontSize — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one.",
   "series[Target range].markArea.label.fontFamily — FINDING 5: the band is drawn the SHIPPED WORKSTATION way on both projections. Round 8 drew this one fact twice — `TARGET 70–180 mg/dL` in caps over dashed edges under `By clock`, `target 70–180` in lowercase over a bare fill under `By event` — so the toggle that promised to hold the data still restyled the band. The lens copy now carries the workstation's fill, its knock-out caption and its dashed edges; against the SHIPPED LENS that reads as a difference, and it is the right one."
  ],
  "pooledOptionExpected": [
   "legend — FINDING 5: ECharts' own legend floated over the plot area. Its keys are read back off this same option and drawn as chips in the canvas header rail beside the title, where the window meta already lives, so the plot carries data and the head carries the explanation.",
   "yAxis[0].max — FINDING 5: 240, not 300. The shipped domain left the top 40% of the largest element on the page rendering nothing; 240 is the first multiple of 40 above the highest value anything on either projection draws (226), so the max sits ON the ladder and roughly 7% is empty above the data.",
   "yAxis[0].interval — FINDING 5: 40, not 60 — one ladder shared with the lens, and the interval that keeps 70 and 180 off it. See the `yAxis.interval` entry in EXPECTED_OPTION.",
   "series[8 Occurrences].itemStyle.color — FINDING 18: one token pair for the two dot states. The shipped scatter draws at `--primary-600`, which on bone is a near-full-contrast dark teal — so unselected dots were LOUDER in light than the accent marking the ones being read, and the emphasis hierarchy inverted between themes. `--fer-dot` is the recessive half, tuned per theme against its own ground."
  ],
  "geometry": {
   "pageScrollX": 0,
   "pageScrollY": 0,
   "levelScrollsInternally": false,
   "breadcrumbClipped": false,
   "unresolvedTokens": []
  }
 },
 "mockProblems": [],
 "openerProblems": []
}
