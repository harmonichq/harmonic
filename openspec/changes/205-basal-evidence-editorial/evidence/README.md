# Evidence — basal evidence goes editorial

Same-fixture before/after, both themes, focal seat at 1440x900, rendered by the component harness from the committed manufactured payload (no real data):

- `basal-clock-before-*.png` — the retired clock treatment at base `5bc3020f`.
- `basal-editorial-after-*.png` — the editorial treatment; the dark after also shows the absent-estimate degradation the fixture carries.

Hardening: the harness sweep rendered the editorial treatment against every real slot of the operator's local snapshot plus eight manufactured edge payloads (n=1 through n=30, all-ties, all-less, absent estimate, degenerate interval, hold, supported raise) with a text bounding-box collision audit: 55/55 clean. The sweep tooling is session scaffolding; the collision audit itself is committed as a unit test.
