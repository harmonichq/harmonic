# Tasks

- [ ] Seed the Diagnose workstation's staged basal, I:C and ISF marks from the shell's `isStaged` verdict when the surface boots, so the dock, the lane and the parameter panel report the persisted Plan draft.
- [ ] Give the app-only opener an opt-in stateful Plan-draft stub, leaving its default stateless empty draft unchanged for every existing story.
- [ ] Add the browser regression story: a change staged in Diagnose still reads as staged after a reload, and can still be un-staged from Diagnose.
