# Evidence — glucose chart legibility (#253)

All renders use committed synthetic inputs. No personal or production health
data appears here.

## Provenance

- Base: chunk-2 integration base `08e8408`, after the clock-gate containment
  chunk and before the palette/composited-audit chunk.
- Revision: merged ticket branch after both implementation chunks.
- App source: `mockups/revise-e2e.synthetic/harmonic.sqlite`, generated by
  `scripts/gen_revise_e2e_db.py`, served only through the repository-declared
  `harmonic serve --no-fetch` path.
- Browser payload: `mockups/diagnose-workstation.synthetic/payload.json`, opened
  through the shipped fixture-backed Diagnose browser harness.
- Geometry: 2084×742 for every capture.

## Capture commands

The populated browser audit captured the Base matrix by substituting only the
base frontend tree while keeping the revision harness and synthetic payload:

```sh
PLAYWRIGHT_MODULE=/Users/connor/.cache/harmonic-browser-gate/pw/node_modules/playwright \
VENDOR_DIR=/Users/connor/.cache/harmonic-browser-gate/vendor \
PAYLOAD=mockups/diagnose-workstation.synthetic/payload.json \
DIAGNOSE_FRONTEND_ROOT=/Users/connor/worktrees/harmonic/253/frontend \
DIAGNOSE_EVIDENCE_KIND=base DIAGNOSE_CAPTURE_ONLY=1 \
DIAGNOSE_SCREENSHOT_DIR=openspec/changes/glucose-chart-legibility/evidence \
node --test --test-name-pattern='populated 2084' \
frontend/diagnose-workstation.browser.test.mjs
```

Revision used the same command without `DIAGNOSE_FRONTEND_ROOT` and with
`DIAGNOSE_EVIDENCE_KIND=revision`. `DIAGNOSE_CAPTURE_ONLY` is deliberately a
non-verifying screenshot path so the failing Base can be recorded; the normal
browser gate runs without it and executes every assertion.

## Matrix

`glucose-chart-legibility/` contains Base and Revision pairs for:

- no active window in Light and Dark; and
- the Morning window in Light and Dark.

Every file is 2084×742 and comes from the same generated source.

## Visual review

- Revision gate lines and hit affordances stop at the glucose plot x-axis; they
  do not enter the basal strip.
- Morning selection changes chart scope without tinting or obscuring basal
  verdict cells.
- Held, insufficient-evidence, and no-data use solid, stripe, and dot structure
  respectively, and the legend repeats the lane treatment.
- Glucose percentile boundaries, median, target treatment, labels, endpoint
  values, axes, basal strip, and legend remain readable in both themes, with and
  without Morning selected.

The executable audit additionally proves the selection scrim changes final
outside-window pixels in Light and Dark; a transparent scrim fails for the
expected unchanged-pixel reason.

## Final verification

- Backend: 2,104 passed, 1 expected skip.
- Frontend dependency-free suite: 527 passed.
- Strict OpenSpec: 71 items passed.
- Populated Diagnose browser suite: 42 passed.
- App-only Diagnose replay: 141 of 141 stories passed against the isolated
  no-fetch synthetic server.
- ADR numbering, owned identifiers, public allowlist, all twelve backend drift
  checks, and both Node drift checks passed.
- The cockpit-shell browser leg was not required because `frontend/theme.css`
  did not change.
