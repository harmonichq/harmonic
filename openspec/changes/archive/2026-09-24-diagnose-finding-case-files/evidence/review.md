# Review evidence — Diagnose Finding case files (#79)

## Provenance and safe start

- Ticket branch: `codex/79-diagnose-high-spike-case-files`.
- Base source: ticket state before the surface chunk, exported to
  `/private/tmp/harmonic79-base-source`.
- Revision source: reviewed surface commit
  `f58b26b07502d2d64c24db989bc491991e49d249`.
- App data: committed synthetic database
  `mockups/revise-e2e.synthetic/harmonic.sqlite`, generated by
  `scripts/gen_revise_e2e_db.py`.
- Server: `uv run harmonic serve --no-fetch --db
  mockups/revise-e2e.synthetic/harmonic.sqlite --port 8766`.

No live fetch, credentials, personal database, or real patient data participated.
The unrelated listener on port 8765 was not reused or stopped.

## Closed render matrix

The external manifest is `/private/tmp/harmonic-79-evidence/manifest.json`.
It contains exactly 64 unique `(state, phase, viewport, theme)` keys:

- 52 `captured` rows whose PNG paths exist;
- 12 `not_applicable` rows, all and only the declared base-recovery cells;
- eight states: meal clock with `claimed < fired`, selected meal event,
  short-meal High selection, missed-meal High event, correction-pair event,
  active failure preservation, stale shadow refresh, and unavailable Occurrence;
- base and revision phases, wide and narrow viewports, and light and dark themes.

Representative wide/narrow and light/dark captures were inspected for the queue,
Inspector hierarchy, exact clock, event cohorts, selected trace, error placement,
and responsive containment. The revision preserves the shipped cockpit geometry,
theme roles, keyboard grammar, and chart hierarchy. The intended visual change is
confined to server-owned case content and explicit recovery/unavailable messages;
no unrelated redesign or mock/lock artifact was introduced.

## Behavior evidence

- The untouched base passed its 43-story frozen workstation ledger before edits.
- The amended workstation replay passed 58 of 58 stories, including every visible
  row opening, all four Exposure families, exact cohort/clock facts, selected
  correction and High traces, event persistence, active failures, the two-leg
  stale handshake, both supersession points, atomic three-surface swap, and
  unavailable selection.
- The event-comparison replay passed 13 of 13 stories against the app.
- Generated browser handlers deserialize preparation and case-file responses
  independently from one retained synthetic server preparation.
- Source guards reject the retired title map, browser membership join, cohort
  recount, silent row no-op, and event-to-clock fallback.

## Verification

- Backend: `1909 passed, 1 skipped`.
- Frontend dependency-free gate: `383 passed`.
- ADR numbering, owned identifiers, public allowlist, synthetic contamination,
  the committed database check, all Python fixture/mirror drift checks, and the
  Node event-comparison generator check exited zero.
- All nine documented browser legs passed. Notable closed inventories were:
  workstation browser `14/14`, cockpit shell `12 passed, 2 declared skips`,
  workstation behavior `58/58`, event comparison `13/13`, and Verify `8/8`.
- `git diff --check` passed and the surface worktree was clean at commit.

## Full review

Round 1 independently enumerated 14 Standards criteria and 14 ticket-Spec
criteria. It found response-identity validation, window supersession, visible
queue-refresh error, authoritative roster-count/history, and dead maintenance
path gaps. Each was fixed with executable regression coverage.

The fix re-review converged on both axes with zero remaining findings. A final
isolated workstation replay passed 58/58 after one unrelated parallel Chromium
process was externally closed; the complete deterministic rerun is the result
reported above.
