# Tasks — Over-treated-low verdict band (#90)

## 1. Publish the complete judgment

- [x] Deepen the projection-facing Over-treated-low rebound decision into one
      guarded scan with fired, near, clean, and insufficient outcomes.
- [x] Preserve the current firing bars, 20 mg/dL near band, meal stop,
      refuted-low behavior, split-off ownership, attribution precedence, and
      separate pre-attribution segmentation gate.
- [x] Publish the judgment for every eligible low in model view.
- [x] Prove all four outcomes from invented CGM and bolus inputs without
      hand-setting the verdict under test.

## 2. Reuse it through every server projection

- [x] Remove Event comparison's duplicate Over-treated-low scan and thresholds
      and consume the shared judgment under its unchanged routing precedence.
- [x] Replace the "never clean" regression and hand-injected Python fixture
      premise with analyzer-produced fired, near, clean, outranked, and no-data
      Occurrences.
- [x] Prove the legacy Findings/Event-comparison responses and canonical Finding
      case-file preparation/case endpoints through their public interfaces.
- [x] Reconcile each canonical Finding's five counts with its cohort/evidence
      roster and denominator while preserving schemas, leases, selection, and
      cache behavior.

## 3. Regenerate, replay, and close

- [x] Regenerate every affected analyzer-backed fixture and mirror through its
      existing producer; keep the Diagnose case files explicitly UI-only.
- [x] Replay and re-inventory the frozen Finding-evidence behavior contract
      against the declared no-fetch synthetic app, amending it only for a real
      shipped-surface behavior change.
- [x] Run the complete merged-branch fast gate, all declared drift checks, the
      affected browser suite, both Diagnose replays, and the Event-comparison
      support audit with nonzero coverage.
- [x] Fold the settled requirement into
      `openspec/specs/behavioral-layer/spec.md` if implementation changes the
      baseline contract, and update this record with shipped evidence before
      opening the pull request. Do not merge.
