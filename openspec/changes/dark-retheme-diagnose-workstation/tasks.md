# Tasks — Promote the Diagnose Dark retheme (#255)

## 1. Re-settle and implement the Dark material contract

- [ ] Add fail-first option/style assertions for the two target-rail changes,
      meal-run alpha, token derivation, material ordering, vessel edge, shared
      hero/basal vessel, single Findings seam, and dock/handle roles.
- [ ] Implement the operator-approved Dark ladder through the existing token and
      role owners in `frontend/theme.css`; leave Light bytes unchanged.
- [ ] Replace only the named filled target ranges and meal-run opacities in the
      existing chart builders; preserve data, range, symbols, and interactions.
- [ ] Keep #253/#256 passive basal-state and composited-chart protections green.

## 2. Close generated and browser-contract drift

- [ ] Make the evidence-canvas exploration's frozen theme bytes derive from, or
      fail-closed compare against, the shipped theme sources; regenerate its
      committed HTML and prove `--check` fails on stale source bytes.
- [ ] Run the finding-evidence exploration check and update only source-owned
      generated bytes that actually changed.
- [ ] Extend existing public browser coverage for Dark material order, one vessel
      edge, no doubled seams, shared hero/basal vessel, target rails, dock roles,
      and Light non-regression.
- [ ] Keep the frozen behavior ledger unchanged and its parity guard green.

## 3. Verify and correct the shipped revision

- [ ] Replay the unchanged Diagnose ledger against base and revision through the
      exact no-fetch synthetic server, with a nonzero story count and no skip.
- [ ] Run the affected Day, Diagnose workstation, Diagnose canvas composition,
      and cockpit browser gates with the repository-declared dependencies.
- [ ] Capture and inspect same-fixture synthetic Dark before/after evidence for
      every affected state in the design record, correcting only failures within
      the locked scope.
- [ ] Run the complete fast gate, all documented drift checks, strict OpenSpec,
      and repository guards before the pull request opens.
