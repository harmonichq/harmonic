# Tasks — Diagnose evidence-first layout

## 1. Implement the settled surface and its behavior contract

- [x] 1.1 Reconfirm the base behavior inventory using `behavior-map.md` and the existing frozen ledger. Amend the ledger and app-only replay for the surfaces delta, retaining permanent, attributed absence checks for the retired dock and its controls. Keep immediate row and chart drill-in and the existing return-to-rank-1 behavior. Do not create a preview-only selection state.
- [x] 1.2 Move the real overview and basal lane below the spotlight in `frontend/diagnose-workstation.js` and its existing styles. Keep the visible window scope and add the Adjust window shortcut. Preserve the chart instance's clock geometry, pointer/touch hit tests, readout, resize observation, basal selection, and the inspector's watched-change floor.
- [x] 1.3 Update `queueRows`/`renderFindingsQueue` and the existing row-mini mount path for the consistent queue requirement. Retain existing served title, applicable annotation, denominator, type and tier words. Use the same priced-row geometry and mini-width floor for every rank. Keep tail/Watching rendering and Sift semantics. Prove the changed public output with the existing generated projection fixture.
- [x] 1.4 Replace the dock toggle, docked/raised strip, and dock resize transitions with direct All charts access. Reuse the current explorer/catalog and chart-click route; preserve its complete chart population, Watching behavior, same-generation evidence, focus, scroll and dismissal. Keep single-chart Expand separate. Update public state and browser contracts together; delete dead mode code rather than retaining a dormant alternate path.
- [x] 1.5 Update the affected tests, replay stories, current design documentation and surface index identified in `behavior-map.md`. Run existing generators for source-coupled extracts and commit only required generated output. Do not restyle a historical exploration or alter fixture producers to force acceptance. Complete the logic checks in `verification.md`, then hand the coherent implementation to phase 2.

## 2. Verify the running surface and finish the implementation PR

- [ ] 2.1 Re-run the frozen base contract immediately before comparison. Serve base and revision separately using the repository's declared synthetic copy-then-serve recipe and identical source database bytes; capture the affected states in `verification.md`. Keep the baseline's behavior ledger with the baseline and the amended ledger with the revision.
- [ ] 2.2 Run the complete amended behavior replay and every repository browser gate against the built revision, including fullscreen/dismissal, Watching access, immediate drill/return, row minis, Sift and clock gestures at their new position. Repair reachable failures in the allowed existing implementation and tests; do not skip a story, waive a retired obligation, or report sandbox launch failure as an app defect.
- [ ] 2.3 Review rendered output in the stated matrix. Correct layout, clipping, focus, touch target, chart-readability and short/narrow viewport issues within the settled arrangement and existing theme. Record before/after evidence, dimensions and the exact data source. Exercise every new story as part of the full aggregate replay before accepting it.
- [ ] 2.4 Complete the merge checks in `verification.md`, retain the decision screenshots, and delete the runnable wireframe. Reconcile this change's design record, behavior ledger and surface index with the delivered state. No new fixture bytes may be hand-authored and no user screenshot may be attached.
- [ ] 2.5 Obtain the required targeted code review and ui-craft rendered audit/polish through the installed workflows; resolve verified in-scope findings with the same worker. Open one human-reviewed PR from the ticket branch after its evidence and checks are complete. Stop without merging.
