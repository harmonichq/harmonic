# High-carb sequence response comparison

## ADR 410 — Show observed glucose responses with the existing comparison chart

Connor asked for the chart to answer “How much worse is my glucose after higher-carb eating?” and confirmed “yes exactly”. He then directed inspection of the other Pattern charts and confirmed “yep” to using their response-comparison presentation. His final “yep” accepted that this includes backend evidence work. These approvals settle the direction, not an executable order or a change to detector policy.

The implementation follows the acceptance requirements in `specs/surfaces/spec.md`. This design is a shipped-surface revision, not a new mock or visual lock. It uses the existing Highs after meals and Lows after correcting highs response charts as its visual reference. The existing two aggregate dot panels are replaced for High-carb sequence; their information remains in supporting detail. Repeat eating is unchanged.

## Current facts and first-hour evidence

`evidence/facts.md` is generated by the recorded command and identifies the source seams, current shapes and reusable cohort projector. Triage built both shells and passed the ten existing eating-sequence composition checks. The component harness was viewed at 1280×720 in manufactured mode: Highs after meals, Lows after correcting highs, Late bolus, Over-treated low and High-carb sequence. The Pattern charts displayed response curves; the selected Cause fixtures were sparse. No dense Cause render is claimed.

The baseline preserves the existing `mockups/finding-evidence-routing.behavior.md` stories S151–S158 and their replay functions. The full unrelated ledgers were not run during triage. The repository's focused-while-iterating rule governs; complete affected ledgers run once before the implementation push. The baseline amendment in that existing ledger records the scoped re-inventory. Existing screenshot files remain local and are not required to acquire this plan.

## Safe start

Declaration: `AGENTS.md`, component harness paragraph. Command: `npm install && npm run dev` within `harness/`. Data: manufactured mode, committed synthetic inputs generated by `scripts/gen_eating_sequence_fixtures.py` and the existing response fixture generator. Triage used the equivalent frozen install `npm ci`, then `npm run dev -- --host 127.0.0.1 --port 5173 --strictPort` after checking the port was free. No application process or vendor fetch ran.

Full-app acceptance uses the exact QA copy-then-serve protocol in `AGENTS.md`, with an explicitly copied synthetic database, `--no-fetch` and `--token ''`. Browser processes require the documented host permission. No normal serve or fetch is authorized.

## Public interface and ownership

The current case-file endpoint and `projection.kind = eating-sequence` remain. Only High-carb sequence gains an additive `projection.response` object and observed selection detail; its existing report remains available. The response has its own version, `schema = high-carb-sequence-response-v1`.

Its fields are `schema`, `alignment` (event), `anchor` (kind and label), `window_min`, `source_window`, `scope`, `period`, `summary`, `comparisons`, `comparison`, and `cohorts`. `comparison` carries the reference cohort name and available/unavailable state required by the existing response legend, derived by the backend from that cohort’s served support. `comparisons` carries the selected scope's existing three aggregate rows from the same evaluation. Each of the two cohorts uses the existing `project_cohort` output shape plus a served name. The highest-carb group uses the rendering key `matched` with name `Highest-carb fifth`; the reference uses `comparison` with name `Other sequences`. These style keys do not classify individual episodes as causal matches. No artificial nearly-matched group is introduced.

The exposure producer retains the high-carb selected comparison metadata and its aggregate rows alongside the existing eligible population, from its existing `SequenceEvaluation`. It does not perform another quintile or eligibility pass. Case preparation retains the corresponding filtered glucose from its existing read snapshot. The case projector slices those observations by each retained interval and calls `event_comparison.project_cohort`; it does not reimplement binning, quantiles or point support. A source-data inconsistency uses the existing inconsistent-projection error path, not a plausible substitute curve.

The curve population is the unscoped retained eligible population. The window-specific occurrence roster continues to use `sequence_population` and its witnessed membership. These populations are intentionally different and labeled. Selecting a roster occurrence adds its observed glucose and `comparison_cohort` to its existing detail, using the same anchor and interval as the response. A selected occurrence outside that roster remains unavailable.

The new response is computed only on High-carb sequence case requests. No cache-lifetime or invalidation machinery changes. The existing preparation identity, generation, lease and stale-recovery paths remain in force.

A narrow adapter presents the response to `eventComparisonChartOption`, `eventComparisonGlucoseValues` and `renderEventSurface`. The renderer remains the owner of glucose curves, target range, support treatment, cursor, keyboard readout and selected trace. The adapter selects fields; it computes no evidence. The existing event-case validator is not weakened to admit sequence populations; `validEatingSequenceCase` validates the additive response and its separate population contract.

The backend owns the High-carb stage headline. Use the existing server headline path for the concise glucose-response title and retain the complete coherent response summary in supporting detail. The exposure population retains the selected comparison period and summary for findings projection. Non-high-carb headlines remain unchanged.

On 2026-09-11, Connor reviewed the rendered mobile header and rejected its paragraph-length numerical title. This direct implementation correction supersedes the earlier instruction to put the full comparison summary in the stage headline. Post-sequence views use “Glucose after high-carb eating”; in-sequence views use an accurate during-eating title. The percentages, comparison, sample counts and caveats remain readable in supporting detail. The correction changes presentation, not the underlying comparison.

## Risk contract

- **Must prevent:** secret exposure, irreversible loss of authoritative data, silent incorrect success; fabricated glucose curves; changed detector eligibility, ranking, ownership, support floor or dosing guidance; a caption describing a different population or period than its curve; false-low observations reappearing in the new evidence.
- **Must recover:** preserve the existing typed stale-generation refresh and retry behavior; add no new automatic recovery.
- **Accepted failure:** insufficient observations remain gaps or explicitly unavailable evidence. An inconsistent response stops at the existing visible error surface and can be retried manually. No interpolation substitutes for missing observations.
- **Unsupported:** causal estimates, carb limits, treatment advice, projected future glucose, a new recovery-time metric, re-ranking within a clock window, and redesign of Repeat eating or unrelated Pattern charts.
- **Evidence owed:** public case-file tests for population/period/trace coherence and unchanged analyzer output; generated synthetic parity; browser proof of actual cohort curves, readable units/names/period, retained navigation and selection, and failure states.

Why: this is descriptive evidence that must remain faithful to the user's observations.
Disposition: → issue #410 and this ADR.

## Verification and execution boundaries

Chunk 1 owns backend source metadata, case projection, stage headline and backend tests. Chunk 2 owns the sequence adapter, shared-renderer integration and focused frontend tests. Chunk 3 owns generator/capture updates, replay amendments, rendered evidence and corrections exposed by the browser run. The third chunk may correct earlier files on the same serial branch; none run in parallel.

Only chunk 3 changes frozen replay assertions. S153/S155/S158 retain their semantic contracts but change their High-carb visual assertions from aggregate scatters to response curves plus supporting aggregates; the Repeat eating arms remain. New observed behavior receives explicit stories in the existing hand-listed browser suite and existing ledger. No separate CI-discovered browser file is introduced. Live pointer probes on the stage and fullscreen left the ECharts selected map unchanged after cohort-label clicks; durable cohort toggling was not observed and is not claimed as inherited behavior. The response legend retains the groups and their support, with no new visibility controls.

If a changed serialized Finding requires QA expectations to move, use the existing manufactured QaCase producer path and remeasure the five AGENTS.md budgets without increasing their limits. No new analyzer state is introduced.

The acceptance run uses the repository fast gate and guards from `AGENTS.md`, focused backend and frontend tests while iterating, generated fixture drift checks, the composition and shared event-response browser suites, and the v2 desk browser suite. Before the final push run the complete affected finding-evidence-routing and event-response ledgers once, and the full v2 ledger at both declared sizes once. Do not run port-bound legs concurrently. Collect before/after High-carb evidence at 1280×720, 1440×900 and 390×844, plus full and miniature ranks. Check the same Pattern reference at each size for shared-renderer regressions. The generated full-v2 command matrix in `evidence/facts.md` is the final replay entry point: build both shells and install the API runtime, resolve the browser environment with the existing helper, create its fresh scratch output root, then run its eight commands serially without `--base`. Each output directory must remain outside the checkout and be fresh. Preserve each selection and log; four shard selections must cover the full registry at each viewport with no failures or deferrals. This matrix is planned verification, not a claim that triage ran the full ledger. Existing v1 commands remain in AGENTS.md.

## Scope and document inventory

The closed inventory is generated in `evidence/facts.md`. Integration also found that the existing Findings fixture generator calls the shared sequence compactor and captures the changed High-carb headlines. Completing the original generator-parity requirement therefore includes `frontend/__fixtures__/findings-projection.json` and its existing `mockups/findings-projection.mirror.mjs` transcription. These are dependent fixture corrections within the same response revision; the existing deep-comparison test remains their authority. The existing `frontend/diagnose-high-causes-have-no-alignment.test.js` source-routing assertion accepts the local validated case binding introduced by the adapter; its shared-renderer requirement remains unchanged. Normative additions live only in the spec delta. Update the eating-sequence section in `DESIGN.md`, the existing behavior ledger and `mockups/INDEX.md` to describe the final shipped revision. Archived decision records retain their history. Do not rewrite the standalone aggregate report specification: its aggregate-only promise remains true.

The work fires multiple deliverable artifacts, live run, split-path evidence and lifecycle-gated revision. Three serial chunks separate backend semantics, the shipped consumer, and generated/live evidence. A nearby repository review-memory anchor agrees with separating the live evidence work. There are no new registry or cache-lifetime semantics requiring a fourth backend chunk. Each chunk is a coherent implementation or evidence pass rather than a file-sized task.
