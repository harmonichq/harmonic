# Revision evidence — a merged basal finding stages the whole span it names (#372)

`/ui-craft revise` lane, against the shipped Diagnose workstation. The frozen
contract is `mockups/finding-evidence-routing.behavior.md` plus
`frontend/diagnose-workstation-behavior.replay.mjs`; this change amends the
ledger and adds story **C61**.

## Safe start and data provenance

Declaration read on this branch: `AGENTS.md`, "The data boundary" — the one
permitted offline serve, quoted there as the QA copy-then-serve block. Run from
the ticket worktree on a free port, never against the committed database and
never against a shared server:

```sh
scratch="${TMPDIR:-/tmp}/harmonic-qa-e2e-372.sqlite"
rm -f "$scratch" "$scratch-wal" "$scratch-shm" "$scratch.derived.sqlite"
cp mockups/qa-e2e.synthetic/harmonic.sqlite "$scratch"
uv run harmonic serve --no-fetch --token '' --db "$scratch" --port 8850
```

Named data source: `mockups/qa-e2e.synthetic/harmonic.sqlite`, generated by
`scripts/gen_qa_e2e_db.py`. Synthetic; no real patient data is involved in any
render, readout or number below. The replay's own deterministic API reads come
from `mockups/diagnose-workstation.synthetic/payload.json` and
`frontend/__fixtures__/findings-projection.json`, both generator-authored.

Base for the before/after pair: commit `4d73ee14` on
`372-merged-basal-partial-staging`, which is this change's own pinned source
commit and carries no implementation — so the base renders are the shipped
surface, captured from the same worktree before the fix.

## Viewports and theme

`1440×900` (the ledger's app-side replay viewport) and `1280×800` (its
mock-side read viewport). One theme: ADR 304 retired the light palette, and
S117 is the ledger's retired story for it.

## The state under revision

`GET /api/diagnose/findings` publishes one merged basal row on this database:

```
basal:180-240 | assert | Basal 03:00 to 04:00 · lower
  row current/recommended = None / None
  members = [(180, 0.6, 0.48), (210, 0.6, 0.48)]
```

`GET /api/analyze?window=30` publishes `asserts_move: true`, `current: 0.6`,
`recommended: 0.48`, `safety_status: "lower"` for slot 6 AND slot 7.

## Before and after, driven through the shipped affordance

Each capture opens the Diagnose queue, clicks `.qrow[data-id="basal:180-240"]`,
presses the panel's single `.stagebtn`, reads `/api/plan` back, then presses it
again to undo. Full readouts: `base-readout.json`, `revision-readout.json`.

| | base | revision |
|---|---|---|
| Plan draft after staging | `[key 6 · 180 · 0.6 → 0.48]` | `[key 6 · 180 · 0.6 → 0.48, key 7 · 210 · 0.6 → 0.48]` |
| Plan badge | `1` | `2` |
| Lane cells marked staged | `03:00` | `03:00`, `03:30` |
| Panel scope line | absent | "One of 2 half hours in Basal 03:00 to 04:00 — staging here stages the whole span. Current, Estimate and Recommended below are this half hour's own." |
| Panel numbers | 0.60 → 0.48, this half hour's own | unchanged — still this half hour's own |
| After undo | `[]` | `[]` |
| Console errors / failed requests | none | none |

Renders, one pair per viewport:

- `base-panel-1440x900.png` · `revision-panel-1440x900.png` — the panel as
  opened from the merged row.
- `base-staged-1440x900.png` · `revision-staged-1440x900.png` — the same panel
  after Stage change, with the lane marks, the Plan badge and the dock visible.
- the same four at `1280x800`.

The watch dock on this database is occupied by an active I:C trial, so its
staged-basal line is not observable here. The dock's span-and-pair behaviour is
pinned instead by replay story C61 and by the two new cases in
`frontend/diagnose-workstation.browser.test.mjs`, which drive the committed
findings fixture's own merged run.

## Failing first

`frontend/diagnose-workspaces.test.js`, the merged-run case, against the
unchanged single-slot filter — the full run is in
`failing-first-stage-items-for.txt`:

```
✖ a merged basal finding maps every eligible published member into Plan rows (#372)
  AssertionError: every asserting member of the run stages, and only those
  + actual - expected
    [
      10,
  -   11
    ]
```

After the fix the same test passes, and no existing guard in that file changed.

## Verification

Run in `/Users/connor/worktrees/harmonic/372` after
`uv sync --frozen --extra api --extra sync`. The raw tails are in
`verification.txt` beside this file.
