# Tasks — QA E2E coverage eras

- [x] #192 basal coverage eras and shared contract — Define `AnalyzerRowKey` and family-discriminated `ExpectedAnalyzerRow` from the serialized-row evidence, including exact omissions and I:C `days_observed` only where emitted; add `QaCase.target_family` (`"basal" | "isf" | "ic"`, or `None` for the three existing cases); extend `QaExpectation`/`assert_expectation` to the named target family's whole row set, the exact measured complement of every other family's quiet predicate (all three complements when the target is `None`), window-keyed queue rows and absences, `support: Mapping[AnalyzerRowKey, ExpectedSupport]` for basal directional support, ISF steps, and I:C whole/effective runs, `asserts_move`, the single Fasting ISF row's exact rest-window set keyed by `(date, start, end)`, and one exact projected I:C history series per active identity keyed by identity (the mapping is empty when no identity is active). Author every target-family default and named override, queue expectation, support value, rest window, and history series literally per case, never from `QaExecution` or analyzer output at test time, and perturb at least one default row to prove it is load-bearing; retain `behavioral_rows` and `finding_titles` verbatim. Add `QaCase.recipe` for direct `materialize_case` dispatch, `QaCase.scoped_windows`, recipe-exact span declarations whose imported family floor applies only to pinned maturity outcomes, and recipe-asserted existing-case declarations (`showcase` 30, `setting-recommendation` 12, `behavioral-precedence` 5); add exactly the 12 basal cases in the design's named-case table; after the first representative basal case, project the remaining 11 cases plus the focused-suite total against 90 seconds and stop on breach; generate `test_case_<name with '-' replaced by '_'>` methods that replace the catalog execution loops, carry and decode the original catalog name, and preserve only the literal catalog-tuple and decoded-name-set execution-free guards; guard `gen_qa_e2e_db.py --case <name> --out <path>` provenance-stamped emission with `--out` defaulting only outside named-case mode and require every generator test invocation to preserve `DEFAULT_OUTPUT` bytes and mtime; amend `AGENTS.md` only to permit an emitted case through the existing offline serve; re-record confirmed non-empty manufactured scan-baseline additions; and record literal command output for all five budgets while keeping the committed showcase database bytes untouched.
- [x] #192 ISF and I:C coverage eras — On the shared contract from task 1, add exactly the 3 ISF and 8 I:C cases in the design's named-case table, including direction-only non-stageable ISF, explicit 30-day I:C collecting, and the I:C history-register case; exercise the declared-span contract and its maturity-conditioned family minimum; after measuring one representative case per family, project `Σ over remaining planned cases of (measured representative single-case time for that family) + current focused-suite total` against 90 seconds and stop on a projected breach; re-record any non-empty generated dose-ratio scan delta only after confirming its manufactured values; and record literal command output for all five budgets while keeping the committed showcase database bytes untouched.
- [x] #193 — Add behavioral and verdict-band coverage eras, including the closed occurrence-state vocabulary, exact per-family denominators, and required negative and outranked/silence cases without continuous IOB or injected verdicts.
- [ ] Follow-on ticket replacing the final #194 cut — migrate the CI browser-gates server, case-file route test, and public-tree allowlist pin; prove and retire revise-e2e by the executable-consumer checklist; document in `AGENTS.md` and `CONTEXT.md` how to add or update an era when analyzer/finding behavior lands, how to emit a named case store, and how to use that store for UI decisions; leave this change active in the implementation pull request so `/ticket finalize` archives it after a human merge.
