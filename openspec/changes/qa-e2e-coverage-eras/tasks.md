# Tasks — QA E2E coverage eras

- [ ] #192 basal coverage eras and shared contract — Extend `QaExpectation` and `assert_expectation` to exact analyzer rows and absences, scoped and unscoped queue rows and absences, support values, `asserts_move`, keyed exact ISF rest-window sets across every ISF row (including an expressible empty ISF list), and one exact projected I:C history series per active identity keyed by identity (including an empty set). Add imported-constant-derived spans for coverage cases, give the existing cases literal declarations matching their current recipe extents (`showcase` 30, `setting-recommendation` 12, `behavioral-precedence` 30), generate and catalog-pin one test method per named case, guard `gen_qa_e2e_db.py --case <name> --out <path>` provenance-stamped emission, add every basal era in the design matrix, re-record confirmed manufactured scan-baseline additions, raise only the backend CI timeout from 10 to 15 minutes, and record all five budgets while keeping the committed showcase database byte-identical.
- [ ] #192 ISF and I:C coverage eras — On the shared contract from task 1, add every ISF and I:C era in the design matrix, including direction-only non-stageable ISF, explicit 30-day I:C collecting, and the I:C history-register era; exercise the family span contract, re-record the generated dose-ratio scan baseline from confirmed manufactured values, and record all five budgets while keeping the committed showcase database byte-identical.
- [ ] #193 — Add behavioral and verdict-band coverage eras, including the closed occurrence-state vocabulary, exact per-family denominators, and required negative and outranked/silence cases without continuous IOB or injected verdicts.
- [ ] Follow-on ticket replacing the final #194 cut — migrate the CI browser-gates server, case-file route test, and public-tree allowlist pin; prove and retire revise-e2e by the executable-consumer checklist; document in `AGENTS.md` and `CONTEXT.md` how to add or update an era when analyzer/finding behavior lands, how to emit a named case store, and how to use that store for UI decisions; then archive this change.
