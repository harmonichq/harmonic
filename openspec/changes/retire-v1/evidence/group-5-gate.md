# Group 5 gate — routes with no shipped caller (#417) and the replay's prototype opener

Run on 0ae4fc9421c868f52d6174a10d629cffe7bf396d in a non-sandboxed shell (a seatbelt sandbox cannot launch Chromium; AGENTS.md).

## Browser legs
```
browser-runner.browser.test.mjs   pass 1  fail 0
frontend/desk.browser.test.mjs    pass 40 fail 0   (36.0 s)
frontend/follow-up.browser.test.mjs pass 2 fail 0  (301.4 s)
```

## Complete desk ledger, both accepted sizes
```
1280x720: ledger inventory: {'issued': 142, 'active': 123, 'retired': 19}
1280x720: ledger=142 registry=142 missing=[] extra=[]
1280x720: complete-replay: exit 0, 823.715 s
1280x720: # executed 142 · failed 0 · deferred 0 · selected 142

1440x900: ledger inventory: {'issued': 142, 'active': 123, 'retired': 19}
1440x900: ledger=142 registry=142 missing=[] extra=[]
1440x900: complete-replay: exit 0, 862.220 s
1440x900: # executed 142 · failed 0 · deferred 0 · selected 142
```

## Pull-request gate
```
npm ci && npm run build            frontend/dist/assets/index-DwbTQ_u1.js 2,949.64 kB (unchanged bundle hash)
node --test frontend/**/*.test.js  tests 784 pass 784 fail 0
uv run python -m pytest            2529 passed, 1 skipped (314.5 s)
openspec validate --all --strict   78 passed, 0 failed
check_adr_numbers / check_owned_identifiers / check_public_allowlist  pass
drift: qa-e2e, harmonic-v2 exploration, demo fixtures, event-comparison capture,
       and the nine fixture generators on this branch  all current
```

The package digest (`follow_up_comparison._execution`) hashes every Python file
under the package, so the route retirement re-stamped `focus.json`,
`journey.json` and the QA showcase; all three were regenerated by their
generators in 0ae4fc94.

## R6 amendment, re-run on 738d0499fdb0269ab59a47187d674701bad40c10

CI on c025df1c failed the 1280x720 4/4 ledger shard on R6 alone (`Timed out after
30000 ms … installSegKeys-style Home/End navigation is back`), while the same
story passed locally at both sizes and on the 1440x900 shard. The identical
failure is on main's runs 35737634434, 35622128492 and 35351344918. The
instrument row is rebuilt on every render, so a render landing after R6's focus
detaches the focused button. R6 now waits for the Diagnose composition before
focusing; claim and actions unchanged.
```
1280x720: complete-replay: exit 0, 806.354 s   # executed 142 · failed 0 · deferred 0 · selected 142
1440x900: complete-replay: exit 0, 882.542 s   # executed 142 · failed 0 · deferred 0 · selected 142
```
