# Tasks

## 1. Audit
- [x] 1.1 Render the harness `strip` story on the committed manufactured
      payload in Light and Dark and post the findings list on ticket #204.

## 2. Corrections
- [x] 2.1 Remove the percentile boundary strokes (fills carry the envelope).
- [x] 2.2 Recompose the Dark scrim (0.28 alpha; Light preserved).
- [x] 2.3 Promote the Dark median to lightened primary.
- [x] 2.4 Remove the legend; add `role="img"` + `aria-label` on the chart root.
- [x] 2.5 Extend the public option/DOM tests, each failing first.
- [x] 2.6 Amend the browser suite's composited-legibility assertions to the
      new contract.

## 3. Proof
- [x] 3.1 Fast gate green.
- [x] 3.2 Browser suites and frozen behavior replay green against the no-fetch
      server.
