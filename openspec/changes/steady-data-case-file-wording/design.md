# Design — steady-data basal case-file wording

## Safe-start provenance

`AGENTS.md` declares the revise safe start:

```sh
uv run harmonic serve --no-fetch --db mockups/revise-e2e.synthetic/harmonic.sqlite
```

This change used that generated synthetic database (seed 620) and the committed Diagnose payload generated by `.claude/qa/gen_synthetic_fixtures.py` from a fixed seed. No fetch path or real health data is involved.

## Scope boundary

The committed demo annotation is in scope because the basal case file prints it verbatim. The findings-queue support noun and the unreferenced legacy basal block are outside this wording correction.
