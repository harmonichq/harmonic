# Harmonic public-repository allowlist (#728, cutover plan Phase 1).
#
# Path rules for the tree that ships to harmonichq/harmonic. Grammar and
# matching precedence are documented in scripts/allowlist_rules.py, which
# parses this file — read that module before editing this one. Every rule
# below cites the plan section that dictates it. Default is exclusion: a
# tracked file that matches nothing here does not ship.
#
# This file, the build script, and the check are themselves allowlisted
# (§1.4) so future development happens where the work lives.

# --- The sixteen root files, dispositioned by §1.6.4's table -----------
# .cbmignore is excluded (private indexing tooling) — no rule.
# AGENTS.md ships by exact path: the repo's own copy is rewritten for the
# public audience and BECOMES the public contributor document, so there is one
# source of truth rather than a file conjured into the tree at build time.
# CLAUDE.md is a symlink to it — leave AGENTS.md unruled and that symlink
# dangles in the materialised tree, which the contamination scan reports as an
# unreadable-file FAILURE (it fails closed rather than skipping).
AGENTS.md
pyproject.toml
uv.lock
Dockerfile
.dockerignore
docker-compose.yml
docker-entrypoint.sh
.gitignore
.env.example
README.md
PRODUCT.md
LICENSE
CONTEXT.md
DESIGN.md
CLAUDE.md

# --- .github: the workflow and the tracker's front door (§1.6.4) -------
# One hook config under .github/ is deliberately left out: it invokes local
# tooling that is not part of this tree, so it would be inert. No rule — the
# default exclusion covers it.
.github/workflows/ci.yml
.github/ISSUE_TEMPLATE/bug.yml
.github/ISSUE_TEMPLATE/config.yml
.github/ISSUE_TEMPLATE/enhancement.yml

# --- The two largest trees, pure Python (§1.6.8) ------------------------
ciq_autotune/** {.py}
tests/** {.py}

# --- The frontend SPA (§1.3). .json is deliberate: it's the entire
# __fixtures__/ set, including the two stamped exemplars and whatever
# Phase 0.5.1 regenerates. The global binary deny and the Phase 3.1
# structural scan are what keep a future real-data JSON out of this rule.
frontend/** {.html,.js,.mjs,.css,.svg,.json}

# --- The public "why" (§0.2 [R5]) ---------------------------------------
openspec/specs/** {.md}

# --- Repo tooling (§1.6.2) -----------------------------------------------
scripts/** {.py,.mjs}
githooks/pre-push

# --- docs/kb/, shipped as-is (§1.6.1). Four named files, not a directory
# glob: a fifth article isn't yet ruled and shouldn't silently ship.
docs/kb/start-here.md
docs/kb/reading-day.md
docs/kb/reading-diagnose.md
docs/kb/the-plan-tab.md

# --- mockups/ is exact-path only (§1.6.6); nothing else in the tree ships
# from that directory.
mockups/explore-investigation.fixture.js
mockups/diagnose-event-comparison-support-audit.mjs
# The fixture-only JS mirror of the findings projection. It is not a mockup
# record: five SHIPPING modules import it, so leaving it out makes the tree
# fail to build. frontend/findings-projection-mirror.test.js (the fast frontend
# job) plus the Diagnose workstation browser gate, the Diagnose workstation and
# event-comparison behaviour-ledger replays all resolve this path.
mockups/findings-projection.mirror.mjs
# The three committed synthetic sets, extension-scoped (§1.6.7).
mockups/diagnose-workstation.synthetic/** {.json,.mjs}
mockups/verify-660-story.synthetic/** {.json,.mjs}
mockups/diagnose-event-comparison.synthetic/** {.json,.mjs}

# --- .claude/: the local demo + generators CI depends on (§1.6, §1.6 [R3])
.claude/launch.json
.claude/qa/gen_synthetic_fixtures.py
.claude/qa/gen_verify_payload.py
.claude/qa/demo_serve.py
.claude/qa/gen_issue_677_capture.py

# --- The allowlist mechanism itself (§1.4). build_public_tree.py and
# check_public_allowlist.py are already cleared by the scripts/** rule
# above; this file needs its own rule because .txt isn't in that extension
# set. Same for the contamination scan's config (§3.1): scan_public_tree.py is
# already cleared by scripts/**, but its config is a .txt.
scripts/public_allowlist.txt
scripts/public_scan_config.txt

# --- The screenshot generators, ruled out AS A CLASS by exact-path deny
# (§0.5.5.4, §1.3 [R9]). frontend/**'s glob would otherwise clear them, and
# two of them carry PHI leaks (basal-lane-394.shot.mjs, dose-focus-385.shot.mjs),
# so the class ships nothing and each member is denied by name. THE RULE IS THE
# CLASS, NOT THE COUNT: every frontend/*.shot.mjs is denied here, and a new one
# is not cleared to ship until it is added to this list and reviewed under the
# ruling. An earlier version of this comment said "nine" and queue-dock-735
# shipped unreviewed on the tenth — do not restate this as a number.
! frontend/basal-lane-394.shot.mjs
! frontend/diagnose-event-comparison-fidelity.shot.mjs
! frontend/diagnose-workstation-fidelity.shot.mjs
! frontend/dialog-a11y-396.shot.mjs
! frontend/dose-focus-385.shot.mjs
! frontend/over-treated-message.shot.mjs
! frontend/plan-first-match-462.shot.mjs
! frontend/plan-nothing-staged.shot.mjs
! frontend/queue-dock-735.shot.mjs
! frontend/verify-660-story-fidelity.shot.mjs
